repeat task.wait() until game:IsLoaded()

-- ============================================================
-- INTRO SEQUENCE (NO BLACK SCREEN, SOUND + ANIMATION KEPT)
-- ============================================================
do
	local _cfg_file = "Dark AngelHubConfig.json"
	local _skip = false
	pcall(function()
		if isfile and isfile(_cfg_file) then
			local raw = readfile(_cfg_file)
			local d = game:GetService("HttpService"):JSONDecode(raw)
			if d.showIntro == false then _skip = true end
		end
	end)

	if not _skip then
		local _TS = game:GetService("TweenService")
		local _PL = game:GetService("Players").LocalPlayer
		local _PG = _PL:WaitForChild("PlayerGui")

		local introGui = Instance.new("ScreenGui")
		introGui.Name = "Dark AngelHubIntro"
		introGui.ResetOnSpawn = false
		introGui.DisplayOrder = 99999
		introGui.IgnoreGuiInset = true
		introGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
		introGui.Parent = _PG

		-- NO BLACK OVERLAY

		local introSound = Instance.new("Sound", introGui)
		introSound.SoundId = "rbxassetid://119414415681261"
		introSound.Volume = 0.85
		introSound.Looped = false
		task.spawn(function()
			task.wait(0.15)
			pcall(function()
				introSound.TimePosition = 45
				introSound:Play()
			end)
		end)

		local lblPhantom = Instance.new("TextLabel", introGui)
		lblPhantom.Size = UDim2.new(0.5,-10,0,60)
		lblPhantom.Position = UDim2.new(-1,0,0.5,-50)
		lblPhantom.BackgroundTransparency = 1
		lblPhantom.Text = "DARK ANGEL"
		lblPhantom.TextColor3 = Color3.fromRGB(255,255,255)
		lblPhantom.Font = Enum.Font.GothamBold
		lblPhantom.TextSize = 8
		lblPhantom.TextXAlignment = Enum.TextXAlignment.Right
		lblPhantom.TextTransparency = 0
		lblPhantom.TextStrokeTransparency = 0.5
		lblPhantom.TextStrokeColor3 = Color3.fromRGB(200,200,200)
		lblPhantom.ZIndex = 2

		local lblHub = Instance.new("TextLabel", introGui)
		lblHub.Size = UDim2.new(0.5,-10,0,60)
		lblHub.Position = UDim2.new(2,0,0.5,-50)
		lblHub.BackgroundTransparency = 1
		lblHub.Text = "HUB"
		lblHub.TextColor3 = Color3.fromRGB(255,255,255)
		lblHub.Font = Enum.Font.GothamBold
		lblHub.TextSize = 8
		lblHub.TextXAlignment = Enum.TextXAlignment.Left
		lblHub.TextTransparency = 0
		lblHub.TextStrokeTransparency = 0.5
		lblHub.TextStrokeColor3 = Color3.fromRGB(200,200,200)
		lblHub.ZIndex = 2

		local lblSub = Instance.new("TextLabel", introGui)
		lblSub.Size = UDim2.new(1,0,0,24)
		lblSub.Position = UDim2.new(0,0,0.5,32)
		lblSub.BackgroundTransparency = 1
		lblSub.Text = "Free | Dark Angel"
		lblSub.TextColor3 = Color3.fromRGB(255,255,255)
		lblSub.Font = Enum.Font.GothamBold
		lblSub.TextSize = 15
		lblSub.TextXAlignment = Enum.TextXAlignment.Center
		lblSub.TextTransparency = 1
		lblSub.ZIndex = 2

		local introLine = Instance.new("Frame", introGui)
		introLine.Size = UDim2.new(0,0,0,2)
		introLine.Position = UDim2.new(0.5,0,0.5,26)
		introLine.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
		introLine.BorderSizePixel = 0
		introLine.ZIndex = 2

		_TS:Create(lblPhantom, TweenInfo.new(0.5, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
			Position = UDim2.new(0,0,0.5,-50), TextSize = 32
		}):Play()
		_TS:Create(lblHub, TweenInfo.new(0.5, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
			Position = UDim2.new(0.5,10,0.5,-50), TextSize = 32
		}):Play()
		task.wait(0.5)

		local lineW = 260
		_TS:Create(introLine, TweenInfo.new(0.45, Enum.EasingStyle.Quad, Enum.EasingDirection.Out), {
			Size = UDim2.new(0,lineW,0,2),
			Position = UDim2.new(0.5,-lineW/2,0.5,26)
		}):Play()
		_TS:Create(lblSub, TweenInfo.new(0.45), {TextTransparency = 0}):Play()

		task.wait(3)

		_TS:Create(lblPhantom, TweenInfo.new(0.4, Enum.EasingStyle.Quad, Enum.EasingDirection.In), {
			Position = UDim2.new(-1,0,0.5,-50), TextSize = 8
		}):Play()
		_TS:Create(lblHub, TweenInfo.new(0.4, Enum.EasingStyle.Quad, Enum.EasingDirection.In), {
			Position = UDim2.new(2,0,0.5,-50), TextSize = 8
		}):Play()
		_TS:Create(lblSub, TweenInfo.new(0.4), {TextTransparency = 1}):Play()
		_TS:Create(introLine, TweenInfo.new(0.4, Enum.EasingStyle.Quad, Enum.EasingDirection.In), {
			Size = UDim2.new(0,0,0,2),
			Position = UDim2.new(0.5,0,0.5,26)
		}):Play()
		task.wait(0.45)
		pcall(function() introSound:Stop() end)
		introGui:Destroy()
	end
end

-- ============================================================
-- DARK ANGEL HUB v5.2 — MEGA AUTO GRAB + ENVY AIMBOT
-- BLACK + CRIMSON RED THEME
-- ============================================================

local Players = game:GetService("Players")
local RunService = game:GetService("RunService")
local UIS = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")
local HttpService = game:GetService("HttpService")
local Stats = game:GetService("Stats")
local Workspace = game:GetService("Workspace")
local LP = Players.LocalPlayer

-- ============================================================
-- MEGA AUTO GRAB STEAL SYSTEM
-- ============================================================
local Steal = {
	AutoStealEnabled = false,
	StealRadius = 60,
	StealDuration = 1.3,
	isStealing = false,
	heartbeatConn = nil,
}

local function getRootPart()
	local char = LP.Character
	return char and (char:FindFirstChild("HumanoidRootPart") or char:FindFirstChild("UpperTorso"))
end

local function isMyPlot(plot)
	if not plot then return false end
	local sign = plot:FindFirstChild("PlotSign")
	if sign then
		local yb = sign:FindFirstChild("YourBase")
		if yb and yb:IsA("BillboardGui") and yb.Enabled then return true end
	end
	return false
end

local function isValidPrompt(prompt)
	if not prompt or not prompt.Parent or not prompt.Enabled then return false end
	local state = prompt:GetAttribute("State")
	local action = prompt.ActionText
	return state == "Steal" or state == "Grab" or action == "Steal" or action == "Grab"
end

local function getClosest()
	local root = getRootPart()
	if not root then return nil end
	local best, bestDist = nil, Steal.StealRadius
	local plots = Workspace:FindFirstChild("Plots")
	if not plots then return nil end
	for _, plot in ipairs(plots:GetChildren()) do
		if isMyPlot(plot) then continue end
		local podiums = plot:FindFirstChild("AnimalPodiums")
		if not podiums then continue end
		for _, podium in ipairs(podiums:GetChildren()) do
			local base = podium:FindFirstChild("Base")
			local spawn = base and base:FindFirstChild("Spawn")
			local att = spawn and spawn:FindFirstChild("PromptAttachment")
			if att then
				for _, child in ipairs(att:GetChildren()) do
					if child:IsA("ProximityPrompt") and isValidPrompt(child) then
						local dist = (root.Position - child.Parent.WorldPosition).Magnitude
						if dist < bestDist then
							bestDist = dist
							best = child
						end
					end
				end
			end
		end
	end
	return best
end

local function fireConnections(prompt, signal)
	local ok, conns = pcall(getconnections, prompt[signal])
	if not ok or not conns then return end
	for _, c in ipairs(conns) do
		if c.Function then task.spawn(c.Function) end
	end
end

local function resetProgressBar()
	if stealPctLbl then stealPctLbl.Text = "READY" end
	if progressFill then progressFill.Size = UDim2.new(0, 0, 1, 0) end
end

local function startAutoSteal()
	if Steal.heartbeatConn then return end
	Steal.heartbeatConn = RunService.Heartbeat:Connect(function()
		if not Steal.AutoStealEnabled or Steal.isStealing then return end
		local prompt = getClosest()
		if not prompt then return end

		Steal.isStealing = true
		if stealPctLbl then stealPctLbl.Text = "STEALING..." end

		fireConnections(prompt, "PromptButtonHoldBegan")

		local startTime = tick()
		local duration = Steal.StealDuration

		while tick() - startTime < duration do
			local progress = (tick() - startTime) / duration
			if progressFill then progressFill.Size = UDim2.new(progress, 0, 1, 0) end
			task.wait()
		end

		if progressFill then progressFill.Size = UDim2.new(1, 0, 1, 0) end

		if prompt and prompt.Parent and prompt.Enabled then
			fireConnections(prompt, "Triggered")
		end

		task.wait(0.15)
		Steal.isStealing = false
		resetProgressBar()
	end)
end

local function stopAutoSteal()
	if Steal.heartbeatConn then Steal.heartbeatConn:Disconnect(); Steal.heartbeatConn = nil end
	Steal.isStealing = false
	resetProgressBar()
end

-- ============================================================
-- ENVY BAT AIMBOT (by @luca19472991)
-- ============================================================
local EnvyAimbot = {
	enabled = false,
	connection = nil,
	target = nil,
	batTool = nil,
	unwalkSavedAnimate = nil,
	equipped = false,
	lastScan = 0,
	speed = 58,
	vertSpeed = 52,
	distance = -2.8,
	height = 4.75,
	vertOffset = 1,
	turnSpeed = 285,
	maxTurnRate = 40,
	swingEnabled = true,
}

-- Helper functions
local function getCharacter() return LP.Character end
local function getHumanoid()
	local char = getCharacter()
	return char and char:FindFirstChildOfClass("Humanoid")
end
local function getRootPart()
	local char = getCharacter()
	return char and char:FindFirstChild("HumanoidRootPart")
end

local function getTarget()
	local root = getRootPart()
	if not root then return nil end
	local now = tick()
	if now - EnvyAimbot.lastScan <= 0.1 and EnvyAimbot.target and EnvyAimbot.target.Parent then
		local hum = EnvyAimbot.target.Parent:FindFirstChildOfClass("Humanoid")
		if hum and hum.Health > 0 then return EnvyAimbot.target end
	end
	EnvyAimbot.lastScan = now
	EnvyAimbot.target = nil
	local closest, minDist = nil, math.huge
	for _, plr in ipairs(Players:GetPlayers()) do
		if plr ~= LP and plr.Character then
			local tRoot = plr.Character:FindFirstChild("HumanoidRootPart")
			local hum = plr.Character:FindFirstChildOfClass("Humanoid")
			if tRoot and hum and hum.Health > 0 then
				local dist = (tRoot.Position - root.Position).Magnitude
				if dist < minDist then
					minDist = dist
					closest = tRoot
				end
			end
		end
	end
	EnvyAimbot.target = closest
	return EnvyAimbot.target
end

local function findBat()
	local char = getCharacter()
	if not char then return nil end
	for _, tool in ipairs(char:GetChildren()) do
		if tool:IsA("Tool") then
			local name = tool.Name:lower()
			if name:find("bat") or name:find("slap") then return tool end
		end
	end
	local bp = LP:FindFirstChildOfClass("Backpack") or LP:FindFirstChild("Backpack")
	if bp then
		for _, tool in ipairs(bp:GetChildren()) do
			if tool:IsA("Tool") then
				local name = tool.Name:lower()
				if name:find("bat") or name:find("slap") then return tool end
			end
		end
	end
	return nil
end

local function ensureBatEquipped()
	local char = getCharacter()
	local hum = getHumanoid()
	if not char or not hum then return end
	if not char:FindFirstChildOfClass("Tool") then
		local bat = findBat()
		if bat then
			pcall(function() hum:EquipTool(bat) end)
			EnvyAimbot.batTool = bat
		end
	else
		EnvyAimbot.batTool = char:FindFirstChildOfClass("Tool")
	end
end

local function resetMotion()
	local root = getRootPart()
	local hum = getHumanoid()
	if root then
		root.AssemblyLinearVelocity = root.AssemblyLinearVelocity * 0.3
		root.AssemblyAngularVelocity = Vector3.zero
	end
	if hum then hum.AutoRotate = true end
end

-- Unwalk
local function startUnwalk()
	local char = getCharacter()
	if not char then return end
	local hum = getHumanoid()
	if hum then
		for _, track in pairs(hum:GetPlayingAnimationTracks()) do
			track:Stop()
		end
	end
	local animate = char:FindFirstChild("Animate")
	if animate then
		EnvyAimbot.unwalkSavedAnimate = animate:Clone()
		animate:Destroy()
	end
end

local function stopUnwalk()
	local char = getCharacter()
	if char and EnvyAimbot.unwalkSavedAnimate then
		EnvyAimbot.unwalkSavedAnimate.Parent = char
		EnvyAimbot.unwalkSavedAnimate = nil
	end
end

local function startEnvyAimbot()
	if EnvyAimbot.connection then return end
	startUnwalk()
	EnvyAimbot.connection = RunService.Heartbeat:Connect(function()
		if not EnvyAimbot.enabled then return end
		local char = getCharacter()
		local hum = getHumanoid()
		local root = getRootPart()
		if not char or not hum or not root then return end
		if not EnvyAimbot.equipped then
			EnvyAimbot.equipped = true
			ensureBatEquipped()
		end
		local target = getTarget()
		if target then
			local targetVel = target.AssemblyLinearVelocity
			local predictTime = math.clamp(targetVel.Magnitude / 130, 0.05, 0.15)
			local aimTargetPos = target.Position + (targetVel * predictTime) + Vector3.new(0, EnvyAimbot.vertOffset, 0)
			hum.AutoRotate = false
			local look = aimTargetPos - root.Position
			local flatLook = Vector3.new(look.X, 0, look.Z)
			if look.Magnitude > 0.01 and flatLook.Magnitude > 0.01 then
				local targetYaw = math.deg(math.atan2(-flatLook.X, -flatLook.Z))
				local yawDelta = (targetYaw - root.Orientation.Y + 180) % 360 - 180
				local targetPitch = math.deg(math.atan2(look.Y, flatLook.Magnitude))
				local pitchDelta = (targetPitch - root.Orientation.X + 180) % 360 - 180
				local yawRate = math.clamp(math.rad(yawDelta) * EnvyAimbot.turnSpeed, -EnvyAimbot.maxTurnRate, EnvyAimbot.maxTurnRate)
				local pitchRate = math.clamp(math.rad(pitchDelta) * EnvyAimbot.turnSpeed, -EnvyAimbot.maxTurnRate, EnvyAimbot.maxTurnRate)
				local yawRad = math.rad(root.Orientation.Y)
				local rightAxis = Vector3.new(math.cos(yawRad), 0, -math.sin(yawRad))
				root.AssemblyAngularVelocity = Vector3.new(0, yawRate, 0) + (rightAxis * pitchRate)
			else
				root.AssemblyAngularVelocity = Vector3.zero
			end
			local dir = look.Magnitude > 0.01 and look.Unit or Vector3.zero
			local standPos = aimTargetPos - (dir * EnvyAimbot.distance) + Vector3.new(0, EnvyAimbot.height, 0)
			local moveDir = standPos - root.Position
			local hDir = Vector3.new(moveDir.X, 0, moveDir.Z)
			local hVel = hDir.Magnitude > 0.1 and hDir.Unit * EnvyAimbot.speed or Vector3.zero
			local vVel = math.abs(moveDir.Y) > 0.1 and Vector3.new(0, math.sign(moveDir.Y) * EnvyAimbot.vertSpeed, 0) or Vector3.new(0, -2, 0)
			root.AssemblyLinearVelocity = hVel + vVel
			if hDir.Magnitude > 0.5 then
				hum:Move(hDir.Unit, false)
			end
			if EnvyAimbot.swingEnabled and (root.Position - target.Position).Magnitude < 6 then
				local bat = findBat() or EnvyAimbot.batTool
				if bat and bat:IsA("Tool") then
					pcall(function() bat:Activate() end)
				end
			end
		else
			hum.AutoRotate = true
			root.AssemblyAngularVelocity = Vector3.zero
			root.AssemblyLinearVelocity = Vector3.zero
		end
	end)
end

local function stopEnvyAimbot()
	if EnvyAimbot.connection then
		EnvyAimbot.connection:Disconnect()
		EnvyAimbot.connection = nil
	end
	resetMotion()
	EnvyAimbot.equipped = false
	stopUnwalk()
end

local function setEnvyAimbotState(enabled)
	EnvyAimbot.enabled = enabled
	if enabled then
		startEnvyAimbot()
	else
		stopEnvyAimbot()
	end
end

-- ============================================================
-- ELDER ANIMATION PACK (unchanged)
-- ============================================================
local lockInEnabled = false
local lockInHeartbeatConn = nil
local originalAnims = nil

local Anims = {
	idle1    = "rbxassetid://10921101664",
	idle2    = "rbxassetid://10921102574",
	walk     = "rbxassetid://10921111375",
	run      = "rbxassetid://10921104374",
	jump     = "rbxassetid://10921107367",
	fall     = "rbxassetid://10921105765",
	climb    = "rbxassetid://10921100400",
	swim     = "rbxassetid://10921108971",
	swimidle = "rbxassetid://10921110146",
}

local function isPackAnim(id)
	if not id then return false end
	for _, v in pairs(Anims) do if v == id then return true end end
	return false
end

local function saveOriginalAnims(char)
	local animate = char:FindFirstChild("Animate")
	if not animate then return end
	local function g(obj) return obj and obj.AnimationId or nil end
	local ids = {
		idle1    = g(animate.idle     and animate.idle.Animation1),
		idle2    = g(animate.idle     and animate.idle.Animation2),
		walk     = g(animate.walk     and animate.walk.WalkAnim),
		run      = g(animate.run      and animate.run.RunAnim),
		jump     = g(animate.jump     and animate.jump.JumpAnim),
		fall     = g(animate.fall     and animate.fall.FallAnim),
		climb    = g(animate.climb    and animate.climb.ClimbAnim),
		swim     = g(animate.swim     and animate.swim.Swim),
		swimidle = g(animate.swimidle and animate.swimidle.SwimIdle),
	}
	if not isPackAnim(ids.walk) then originalAnims = ids end
end

local function applyAnimPack(char)
	local animate = char:FindFirstChild("Animate")
	if not animate then return end
	local function s(obj, id) if obj then obj.AnimationId = id end end
	s(animate.idle     and animate.idle.Animation1,     Anims.idle1)
	s(animate.idle     and animate.idle.Animation2,     Anims.idle2)
	s(animate.walk     and animate.walk.WalkAnim,       Anims.walk)
	s(animate.run      and animate.run.RunAnim,         Anims.run)
	s(animate.jump     and animate.jump.JumpAnim,       Anims.jump)
	s(animate.fall     and animate.fall.FallAnim,       Anims.fall)
	s(animate.climb    and animate.climb.ClimbAnim,     Anims.climb)
	s(animate.swim     and animate.swim.Swim,           Anims.swim)
	s(animate.swimidle and animate.swimidle.SwimIdle,   Anims.swimidle)
end

local function restoreOriginalAnims(char)
	if not originalAnims then return end
	local animate = char:FindFirstChild("Animate")
	if not animate then return end
	local function s(obj, id) if obj and id then obj.AnimationId = id end end
	s(animate.idle     and animate.idle.Animation1,     originalAnims.idle1)
	s(animate.idle     and animate.idle.Animation2,     originalAnims.idle2)
	s(animate.walk     and animate.walk.WalkAnim,       originalAnims.walk)
	s(animate.run      and animate.run.RunAnim,         originalAnims.run)
	s(animate.jump     and animate.jump.JumpAnim,       originalAnims.jump)
	s(animate.fall     and animate.fall.FallAnim,       originalAnims.fall)
	s(animate.climb    and animate.climb.ClimbAnim,     originalAnims.climb)
	s(animate.swim     and animate.swim.Swim,           originalAnims.swim)
	s(animate.swimidle and animate.swimidle.SwimIdle,   originalAnims.swimidle)
	local hum2 = char:FindFirstChildOfClass("Humanoid")
	if hum2 then
		for _, track in ipairs(hum2:GetPlayingAnimationTracks()) do track:Stop(0) end
		hum2:ChangeState(Enum.HumanoidStateType.Running)
	end
end

task.spawn(function() end)

local _isfile = isfile or (syn and syn.isfile) or (getgenv and getgenv().isfile) or function() return false end
local _readfile = readfile or (syn and syn.readfile) or (getgenv and getgenv().readfile) or function() return nil end
local _writefile= writefile or (syn and syn.writefile) or (getgenv and getgenv().writefile) or function() end
local getconnections = getconnections or get_signal_cons or getconnects or (syn and syn.get_signal_cons)

-- MODULE-LEVEL VARIABLES
local duelDir = "left"
local bypassActivated = false
local bypassPower = 300000
local bypassLagAmount = 0.12
local bypassConn = nil
local setBypassToggle = nil
local bypassPowerBox = nil

local function applyBypassPower(val)
	bypassPower = math.clamp(val, 10000, 500000)
	local t2 = (bypassPower - 10000) / 490000
	bypassLagAmount = t2 * 0.2
	if bypassPowerBox then bypassPowerBox.Text = tostring(bypassPower) end
end

local function startBypassLag()
	if bypassConn then bypassConn:Disconnect() end
	bypassConn = RunService.RenderStepped:Connect(function()
		if not bypassActivated then return end
		if bypassLagAmount > 0 then
			local t2 = tick()
			while tick() - t2 < bypassLagAmount do end
		end
	end)
end

local function stopBypassLag()
	bypassActivated = false
	if bypassConn then bypassConn:Disconnect(); bypassConn = nil end
end

applyBypassPower(bypassPower)

LP.CharacterAdded:Connect(function()
	task.wait(1)
	if bypassActivated then
		stopBypassLag()
		bypassActivated = true
		startBypassLag()
	end
end)

local currentFOV = 70
local setFOVExternal = nil
local activeSkyMode = "None"
local applySkyMode = nil

-- STATE
local State = {
	normalSpeed=60,
	carrySpeed=30,
	laggerSpeed=10.1,
	speedToggled=false,
	laggerEnabled=false,
	infJumpEnabled=false,
	jumpPower=55,
	antiRagdollEnabled=false,
	fpsBoostEnabled=false,
	guiVisible=true,
	uiLocked=false,
	autoLeftEnabled=false,
	autoRightEnabled=false,
	autoLeftPhase=1,
	autoRightPhase=1,
	waypointIdx=1,
	medusaLastUsed=0,
	medusaDebounce=false,
	medusaCounterEnabled=false,
	batCounterEnabled=false,
	batCounterDebounce=false,
	dropEnabled=false,
	_tpInProgress=false,
	lastMoveDir=Vector3.new(0,0,0),
	unwalkEnabled=false,
	stackButtonsHidden=false,
	_prevCarry=30,
	_prevSpeed=false,
	duelCountdownEnabled=false,
	_duelWaiting=false,
	savedButtonPositions = {},
	prcMode = "Default",
	tuntActive = false,
	tuntLoop = nil,
	autoTpDownEnabled = false,
	autoTpDownY = 20,

	-- PERSISTENT TOGGLES
	stretchRez = false,
	nightTime = false,
	purpleSky = false,
	fpsBoosterCursed = false,
	playerEsp = false,
	antiLagClean = false,
	cloudyTime = false,
	darkModeEnabled = false,
	optimizeEnabled = false,
	xrayEnabled = false,
	
	-- SCALES
	uiScale = 0.9,
	windowScale = 0.9,

	-- INTRO
	showIntro = true,
}

-- KEYBINDS
local Keys = {
	speed=Enum.KeyCode.Q,
	guiHide=Enum.KeyCode.LeftControl,
	autoLeft=Enum.KeyCode.L,
	autoRight=Enum.KeyCode.R,
	lagger=Enum.KeyCode.Unknown,
	tpDown=Enum.KeyCode.Unknown,
	drop=Enum.KeyCode.H,
	aimbot=Enum.KeyCode.Unknown,
}

-- DEFAULT STACK BUTTON POSITIONS
local BTN_W=58; local BTN_H=58; local BTN_GAP=6; local COLS=2
local stackDefs = {
	{key="autoLeft", label="AUTO\nLEFT"},
	{key="autoRight", label="AUTO\nRIGHT"},
	{key="aimbot", label="BAT\nAIMBOT"},
	{key="lagger", label="LAGGER\nCARRY"},
	{key="drop", label="DROP\nBR"},
	{key="tpDown", label="TP\nDOWN"},
	{key="carrySpeed", label="CARRY\nSPEED"},
	{key="tunt", label="TAUNT"},
}
local GRID_W=COLS*(BTN_W+BTN_GAP)-BTN_GAP
local GRID_H=math.ceil(#stackDefs/COLS)*(BTN_H+BTN_GAP)-BTN_GAP
local function getDefaultStackPos(i)
	local col=(i-1)%COLS
	local row2=math.floor((i-1)/COLS)
	return UDim2.new(1,-(GRID_W+14)+col*(BTN_W+BTN_GAP),0.5,-(GRID_H/2)+row2*(BTN_H+BTN_GAP))
end

local MOVE_KEYS={[Enum.KeyCode.W]=true,[Enum.KeyCode.A]=true,[Enum.KeyCode.S]=true,[Enum.KeyCode.D]=true,
	[Enum.KeyCode.Up]=true,[Enum.KeyCode.Down]=true,[Enum.KeyCode.Left]=true,[Enum.KeyCode.Right]=true}
local MEDUSA_COOLDOWN=25; local DROP_AUTO_OFF_DELAY=0.15
local POS={
	L1=Vector3.new(-476.85,-6.59,94.91), L2=Vector3.new(-485.55,-4.53,100.61),
	R1=Vector3.new(-475.77,-6.57,26.76), R2=Vector3.new(-485.85,-4.48,20.13),
	-- SZG Waypoints for multi-waypoint patrol
	leftWaypoints = {
		Vector3.new(-476.85, -6.59, 94.91),
		Vector3.new(-485.55, -4.53, 100.61),
	},
	rightWaypoints = {
		Vector3.new(-475.77, -6.57, 26.76),
		Vector3.new(-485.85, -4.48, 20.13),
	},
}

local Conns={autoSteal=nil,antiRag=nil,autoLeft=nil,autoRight=nil,anchor={},progress=nil,batCounter=nil,unwalk=nil}
local h,hrp
local setAutoLeft,setAutoRight,setInfJump,setAntiRag,setFps
local setMedusaCounter,setUnwalkToggle,setBatAimbot,setBatCounter
local setLagger,setDropBrainrot,setInstaGrab
local startESP, stopESP
local setupMedusaCounter,stopMedusaCounter,startAntiRagdoll,stopAntiRagdoll
local applyFPSBoost
local startAutoLeft,stopAutoLeft,startAutoRight,stopAutoRight
local saveConfig,loadConfig,runDropBrainrot,stopDropBrainrot,doTpDown
local startBatCounter,stopBatCounter
local stackBtnRefs={}; local stackWrappers={}; local keybindBtnRefs={}
local normalBox,carryBox,laggerBox,stealRadBox,lockBtn
local setHideButtonsToggle
local setShowIntroToggle
local setLockInToggle
local radTB
local stealPctLbl, progressFill, radLbl  -- for progress bar

-- ============================================================
-- THEME: BLACK + CRIMSON RED
-- ============================================================
local C = {
	winBg = Color3.fromRGB(8, 8, 8),
	winBorder = Color3.fromRGB(255, 255, 255),
	topBg = Color3.fromRGB(10, 10, 10),
	topTitle = Color3.fromRGB(255, 255, 255),
	topSub = Color3.fromRGB(180, 180, 180),
	topBtn = Color3.fromRGB(255, 255, 255),
	topBtnHov = Color3.fromRGB(255, 255, 255),
	topDivider = Color3.fromRGB(255, 255, 255),
	tabBarBg = Color3.fromRGB(12, 12, 12),
	tabBarDiv = Color3.fromRGB(255, 255, 255),
	tabIdle = Color3.fromRGB(150, 150, 150),
	tabActive = Color3.fromRGB(255, 255, 255),
	tabActiveBg = Color3.fromRGB(20, 20, 20),
	tabUnderline = Color3.fromRGB(255, 255, 255),
	sectionTxt = Color3.fromRGB(255, 255, 255),
	sectionDiv = Color3.fromRGB(255, 255, 255),
	rowBg = Color3.fromRGB(5, 5, 5),
	rowBorder = Color3.fromRGB(255, 255, 255),
	rowLabel = Color3.fromRGB(255, 255, 255),
	rowSub = Color3.fromRGB(180, 180, 180),
	rowValue = Color3.fromRGB(255, 255, 255),
	rowHov = Color3.fromRGB(25, 25, 25),
	inputBg = Color3.fromRGB(12, 12, 12),
	inputBorder = Color3.fromRGB(255, 255, 255),
	inputFocus = Color3.fromRGB(255, 255, 255),
	inputTxt = Color3.fromRGB(255, 255, 255),
	pillOff = Color3.fromRGB(30, 30, 30),
	pillOn = Color3.fromRGB(0, 255, 100),
	dotOff = Color3.fromRGB(80, 80, 80),
	dotOn = Color3.fromRGB(0, 255, 100),
	pillBorder = Color3.fromRGB(255, 255, 255),
	modeBtnBg = Color3.fromRGB(15, 15, 15),
	modeBtnBrd = Color3.fromRGB(255, 255, 255),
	modeBtnTxt = Color3.fromRGB(255, 255, 255),
	modeBtnActBg = Color3.fromRGB(30, 30, 30),
	modeBtnActTx = Color3.fromRGB(0, 255, 100),
	chipBg = Color3.fromRGB(20, 20, 20),
	chipBorder = Color3.fromRGB(255, 255, 255),
	chipTxt = Color3.fromRGB(255, 255, 255),
	btnBg = Color3.fromRGB(25, 25, 25),
	btnBorder = Color3.fromRGB(255, 255, 255),
	btnTxt = Color3.fromRGB(255, 255, 255),
	btnHov = Color3.fromRGB(50, 50, 50),
	stackBg = Color3.fromRGB(15, 15, 15),
	stackBrd = Color3.fromRGB(255, 255, 255),
	stackTxt = Color3.fromRGB(160, 160, 160),
	stackActBg = Color3.fromRGB(255, 255, 255),
	stackActTxt = Color3.fromRGB(255, 255, 255),
	stackActBrd = Color3.fromRGB(220, 220, 220),
	stackDot = Color3.fromRGB(60, 60, 60),
	stackDotOn = Color3.fromRGB(0, 255, 100),
	infoBg = Color3.fromRGB(8, 8, 8),
	infoBrd = Color3.fromRGB(255, 255, 255),
	infoTxt = Color3.fromRGB(255, 255, 255),
	infoVal = Color3.fromRGB(0, 255, 100),
	infoFill = Color3.fromRGB(100, 100, 100),
	accent = Color3.fromRGB(255, 255, 255),
	accentDim = Color3.fromRGB(180, 180, 180),
	lockOn = Color3.fromRGB(200, 200, 200),
	divider = Color3.fromRGB(255, 255, 255),
	flashRed = Color3.fromRGB(200, 200, 200),
}

-- PRC PERFORMANCE MODES
local function applyPRCMode(mode)
	State.prcMode = mode
	if mode == "Default" then
		pcall(function() setfpscap(60) end)
		pcall(function()
			local L = game:GetService("Lighting")
			L.GlobalShadows = true
			L.FogEnd = 100000
			L.Brightness = 1
		end)
	elseif mode == "Normal" then
		pcall(function() setfpscap(120) end)
		pcall(function()
			local L = game:GetService("Lighting")
			L.GlobalShadows = false
			L.FogEnd = 9e9
		end)
	elseif mode == "High" then
		pcall(function() setfpscap(999999999) end)
		applyFPSBoost()
	end
end

-- CLEANUP
for _,name in pairs({"VyseSlottedGUI","VyseAsireGUI","VyseAsireHubV4","VyseAsireHubV5","VyseAsireHubV5_1","Dark AngelHubV5_1","AsireHubV5_2","DARK ANGELHUBV5_2"}) do
	pcall(function() local o=game:GetService("CoreGui"):FindFirstChild(name); if o then o:Destroy() end end)
	pcall(function() local o=LP:WaitForChild("PlayerGui"):FindFirstChild(name); if o then o:Destroy() end end)
end

-- ROOT GUI
local gui=Instance.new("ScreenGui")
gui.Name="DARK ANGELHUBV5_2"; gui.ResetOnSpawn=false; gui.DisplayOrder=10
gui.IgnoreGuiInset=true; gui.ZIndexBehavior=Enum.ZIndexBehavior.Sibling
gui.Parent=LP:WaitForChild("PlayerGui")

local globalScaleObj = Instance.new("UIScale", gui)
globalScaleObj.Scale = State.uiScale

local mainOuter = nil

local function mkCorner(p,r)
	local c=Instance.new("UICorner",p); c.CornerRadius=UDim.new(0,r or 6); return c
end
local function mkStroke(p,col,th)
	local s=Instance.new("UIStroke",p); s.Color=col; s.Thickness=th or 1
	s.ApplyStrokeMode=Enum.ApplyStrokeMode.Border; return s
end

-- CRIMSON RED ANIMATED GRADIENT STROKE
local function attachAnimStroke(parent)
	local s = Instance.new("UIStroke", parent)
	s.ApplyStrokeMode = Enum.ApplyStrokeMode.Border
	s.LineJoinMode = Enum.LineJoinMode.Round
	s.Thickness = 2
	s.Transparency = 0
	s.Color = Color3.new(1, 1, 1)
	local g = Instance.new("UIGradient", s)
	g.Color = ColorSequence.new({
		ColorSequenceKeypoint.new(0,   Color3.fromRGB(0, 0, 0)),
		ColorSequenceKeypoint.new(0.4, Color3.fromRGB(40, 40, 40)),
		ColorSequenceKeypoint.new(0.5, Color3.fromRGB(255, 255, 255)),
		ColorSequenceKeypoint.new(0.6, Color3.fromRGB(40, 40, 40)),
		ColorSequenceKeypoint.new(1,   Color3.fromRGB(0, 0, 0)),
	})
	g.Rotation = 0
	RunService.Heartbeat:Connect(function()
		local time = tick()
		g.Rotation = (time * 40) % 360
		local pulse = math.sin((time * 0.8) * math.pi * 2)
		s.Transparency = 0.1 * (1 - ((pulse + 1) / 2))
	end)
	return s
end

-- DRAG FUNCTIONS
local function makeDraggable(frame,handle)
	local src=handle or frame
	local dragging,dragInput,dragStart,startPos=false,nil,nil,nil
	src.InputBegan:Connect(function(inp)
		if State.uiLocked then return end
		if inp.UserInputType==Enum.UserInputType.MouseButton1 or inp.UserInputType==Enum.UserInputType.Touch then
			dragging=true; dragStart=inp.Position; startPos=frame.Position
			inp.Changed:Connect(function()
				if inp.UserInputState==Enum.UserInputState.End then dragging=false end
			end)
		end
	end)
	src.InputChanged:Connect(function(inp)
		if inp.UserInputType==Enum.UserInputType.MouseMovement or inp.UserInputType==Enum.UserInputType.Touch then
			dragInput=inp
		end
	end)
	UIS.InputChanged:Connect(function(inp)
		if inp==dragInput and dragging and not State.uiLocked then
			local dx=inp.Position.X-dragStart.X; local dy=inp.Position.Y-dragStart.Y
			frame.Position=UDim2.new(startPos.X.Scale,startPos.X.Offset+dx,startPos.Y.Scale,startPos.Y.Offset+dy)
		end
	end)
end

local function makeStackDraggable(frame,onTap,buttonKey)
	local dragging,dragInput,dragStart,startPos=false,nil,nil,nil; local moved=false
	frame.InputBegan:Connect(function(inp)
		if inp.UserInputType~=Enum.UserInputType.MouseButton1 and inp.UserInputType~=Enum.UserInputType.Touch then return end
		dragging=true; moved=false; dragStart=inp.Position; startPos=frame.Position
	end)
	frame.InputEnded:Connect(function(inp)
		if inp.UserInputType~=Enum.UserInputType.MouseButton1 and inp.UserInputType~=Enum.UserInputType.Touch then return end
		if dragging then
			if not moved and onTap then onTap() end
			if moved and buttonKey then
				State.savedButtonPositions[buttonKey] = {X=frame.Position.X.Offset, Y=frame.Position.Y.Offset}
				pcall(saveButtonPositions)
			end
		end
		dragging=false; moved=false
	end)
	frame.InputChanged:Connect(function(inp)
		if inp.UserInputType==Enum.UserInputType.MouseMovement or inp.UserInputType==Enum.UserInputType.Touch then
			dragInput=inp
		end
	end)
	UIS.InputChanged:Connect(function(inp)
		if inp~=dragInput or not dragging then return end
		local dx=inp.Position.X-dragStart.X; local dy=inp.Position.Y-dragStart.Y
		if math.abs(dx)>4 or math.abs(dy)>4 then moved=true end
		if moved and not State.uiLocked then
			frame.Position=UDim2.new(startPos.X.Scale,startPos.X.Offset+dx,startPos.Y.Scale,startPos.Y.Offset+dy)
		end
	end)
end

-- SAVE/LOAD BUTTON POSITIONS
local BUTTON_POS_FILE = "Dark AngelHubButtonPos.json"
local function saveButtonPositions()
	local ok, encoded = pcall(function() return HttpService:JSONEncode(State.savedButtonPositions) end)
	if ok then pcall(function() _writefile(BUTTON_POS_FILE, encoded) end) end
end
local function loadButtonPositions()
	local hasFile = false; pcall(function() hasFile = _isfile(BUTTON_POS_FILE) end)
	if not hasFile then return end
	local raw; pcall(function() raw = _readfile(BUTTON_POS_FILE) end)
	if not raw then return end
	local ok, decoded = pcall(function() return HttpService:JSONDecode(raw) end)
	if ok and decoded then
		State.savedButtonPositions = decoded
		for i, def in ipairs(stackDefs) do
			local wrapper = stackWrappers[def.key]
			if wrapper and State.savedButtonPositions[def.key] then
				local pos = State.savedButtonPositions[def.key]
				wrapper.Position = UDim2.new(1, pos.X, 0.5, pos.Y)
			end
		end
	end
end

-- ============================================================
-- MAIN WINDOW - DARK ANGEL HUB STYLE
-- ============================================================
local WIN_W = 420; local WIN_H = 360; local TITLE_H = 48
local SIDEBAR_W = 110

mainOuter = Instance.new("Frame", gui)
mainOuter.Name = "MainOuter"
mainOuter.Size = UDim2.new(0, WIN_W, 0, WIN_H)
mainOuter.Position = UDim2.new(0.5, -WIN_W/2, 0.5, -WIN_H/2)
mainOuter.BackgroundColor3 = Color3.fromRGB(8, 8, 8)
mainOuter.BackgroundTransparency = 0
mainOuter.BorderSizePixel = 0
mainOuter.ClipsDescendants = true
mkCorner(mainOuter, 18)
mkStroke(mainOuter, Color3.fromRGB(255, 255, 255), 1.5)

local windowScaleObj = Instance.new("UIScale", mainOuter)
windowScaleObj.Scale = State.windowScale

local function recenterWindow()
	mainOuter.Position = UDim2.new(0.5, -WIN_W * State.windowScale / 2, 0.5, -WIN_H * State.windowScale / 2)
end

-- TITLE BAR
local titleBar = Instance.new("Frame", mainOuter)
titleBar.Name = "TitleBar"
titleBar.Size = UDim2.new(1, 0, 0, TITLE_H)
titleBar.Position = UDim2.new(0, 0, 0, 0)
titleBar.BackgroundColor3 = Color3.fromRGB(10, 10, 10)
titleBar.BorderSizePixel = 0
titleBar.ZIndex = 5
mkCorner(titleBar, 18)
local titleBarBottom = Instance.new("Frame", titleBar)
titleBarBottom.Size = UDim2.new(1, 0, 0, 18)
titleBarBottom.Position = UDim2.new(0, 0, 1, -18)
titleBarBottom.BackgroundColor3 = Color3.fromRGB(10, 10, 10)
titleBarBottom.BorderSizePixel = 0
titleBarBottom.ZIndex = 5

makeDraggable(mainOuter, titleBar)

local titleLbl = Instance.new("TextLabel", titleBar)
titleLbl.Size = UDim2.new(0, 160, 1, 0)
titleLbl.Position = UDim2.new(0, 16, 0, 0)
titleLbl.BackgroundTransparency = 1
titleLbl.Text = "DARK ANGEL HUB"
titleLbl.TextColor3 = Color3.fromRGB(255, 255, 255)
titleLbl.Font = Enum.Font.GothamBlack
titleLbl.TextSize = 15
titleLbl.TextXAlignment = Enum.TextXAlignment.Left
titleLbl.ZIndex = 6

local subLbl = Instance.new("TextLabel", titleBar)
subLbl.Size = UDim2.new(0, 160, 0, 14)
subLbl.Position = UDim2.new(0, 16, 0, 28)
subLbl.BackgroundTransparency = 1
subLbl.Text = "https://discord.gg/hPmezgtxTU"
subLbl.TextColor3 = Color3.fromRGB(255, 255, 255)
subLbl.Font = Enum.Font.Gotham
subLbl.TextSize = 10
subLbl.TextXAlignment = Enum.TextXAlignment.Left
subLbl.ZIndex = 6

local closeBtn = Instance.new("TextButton", titleBar)
closeBtn.Size = UDim2.new(0, 28, 0, 28)
closeBtn.Position = UDim2.new(1, -40, 0.5, -14)
closeBtn.BackgroundColor3 = Color3.fromRGB(22, 22, 22)
closeBtn.BorderSizePixel = 0
closeBtn.Text = "—"
closeBtn.TextColor3 = Color3.fromRGB(180, 180, 180)
closeBtn.Font = Enum.Font.GothamBold
closeBtn.TextSize = 14
closeBtn.ZIndex = 7
mkCorner(closeBtn, 10)
mkStroke(closeBtn, Color3.fromRGB(255, 255, 255), 1)
closeBtn.MouseButton1Click:Connect(function()
	closeBtn.BackgroundColor3 = C.flashRed
	task.delay(0.1, function() if closeBtn then closeBtn.BackgroundColor3 = Color3.fromRGB(22,22,22) end end)
	hideMenu()
end)

lockBtn = Instance.new("TextButton", titleBar)
lockBtn.Size = UDim2.new(0, 28, 0, 28)
lockBtn.Position = UDim2.new(1, -74, 0.5, -14)
lockBtn.BackgroundColor3 = Color3.fromRGB(22, 22, 22)
lockBtn.BorderSizePixel = 0
lockBtn.Text = "🔒"
lockBtn.TextColor3 = Color3.fromRGB(180, 180, 180)
lockBtn.Font = Enum.Font.GothamBold
lockBtn.TextSize = 11
lockBtn.ZIndex = 7
mkCorner(lockBtn, 10)
mkStroke(lockBtn, Color3.fromRGB(255, 255, 255), 1)
lockBtn.MouseButton1Click:Connect(function()
	lockBtn.BackgroundColor3 = C.flashRed
	task.delay(0.1, function() if lockBtn then lockBtn.BackgroundColor3 = Color3.fromRGB(22,22,22) end end)
	State.uiLocked = not State.uiLocked
	lockBtn.Text = State.uiLocked and "🔒" or "🔓"
	pcall(function() if lockUISync then lockUISync(State.uiLocked) end end)
end)

local titleDiv = Instance.new("Frame", mainOuter)
titleDiv.Size = UDim2.new(1, 0, 0, 1)
titleDiv.Position = UDim2.new(0, 0, 0, TITLE_H)
titleDiv.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
titleDiv.BorderSizePixel = 0
titleDiv.ZIndex = 5

local BODY_Y = TITLE_H + 1

-- LEFT SIDEBAR
local sidebar = Instance.new("Frame", mainOuter)
sidebar.Name = "Sidebar"
sidebar.Size = UDim2.new(0, SIDEBAR_W, 1, -BODY_Y)
sidebar.Position = UDim2.new(0, 0, 0, BODY_Y)
sidebar.BackgroundColor3 = Color3.fromRGB(8, 8, 8)
sidebar.BorderSizePixel = 0
sidebar.ZIndex = 3
mkCorner(sidebar, 18)
local sideCornerFix = Instance.new("Frame", sidebar)
sideCornerFix.Size = UDim2.new(1, 0, 0, 18)
sideCornerFix.Position = UDim2.new(0, 0, 0, 0)
sideCornerFix.BackgroundColor3 = Color3.fromRGB(8, 8, 8)
sideCornerFix.BorderSizePixel = 0
sideCornerFix.ZIndex = 3

local sideLL = Instance.new("UIListLayout", sidebar)
sideLL.SortOrder = Enum.SortOrder.LayoutOrder
sideLL.Padding = UDim.new(0, 2)
local sidePad = Instance.new("UIPadding", sidebar)
sidePad.PaddingTop = UDim.new(0, 8)
sidePad.PaddingBottom = UDim.new(0, 8)
sidePad.PaddingLeft = UDim.new(0, 0)
sidePad.PaddingRight = UDim.new(0, 0)

local sideDiv = Instance.new("Frame", mainOuter)
sideDiv.Size = UDim2.new(0, 1, 1, -BODY_Y)
sideDiv.Position = UDim2.new(0, SIDEBAR_W, 0, BODY_Y)
sideDiv.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
sideDiv.BorderSizePixel = 0
sideDiv.ZIndex = 4

-- RIGHT CONTENT AREA
local contentBg = Instance.new("Frame", mainOuter)
contentBg.Name = "ContentBg"
contentBg.Size = UDim2.new(1, -(SIDEBAR_W + 1), 1, -BODY_Y)
contentBg.Position = UDim2.new(0, SIDEBAR_W + 1, 0, BODY_Y)
contentBg.BackgroundColor3 = Color3.fromRGB(10, 10, 10)
contentBg.BackgroundTransparency = 0
contentBg.BorderSizePixel = 0
contentBg.ClipsDescendants = true
contentBg.ZIndex = 2
mkCorner(contentBg, 18)

local watermark = Instance.new("TextLabel", mainOuter)
watermark.Size = UDim2.new(1, 0, 0, 120)
watermark.Position = UDim2.new(0, 0, 0.5, -20)
watermark.BackgroundTransparency = 1
watermark.Text = "DARK ANGEL"
watermark.TextColor3 = Color3.fromRGB(255, 255, 255)
watermark.TextTransparency = 0.85
watermark.Font = Enum.Font.GothamBlack
watermark.TextScaled = true
watermark.ZIndex = 3
watermark.BorderSizePixel = 0

-- TABS
local TABS = {"Speed", "Aimbot", "Mech", "Keybind", "Dark Angel Visual", "Settings"}
local currentTab = "Speed"
local tabBtns = {}; local tabPages = {}

for i, name in ipairs(TABS) do
	local btn = Instance.new("TextButton", sidebar)
	btn.Name = "Tab_"..name
	btn.Size = UDim2.new(1, 0, 0, 36)
	btn.BackgroundColor3 = (name == currentTab) and Color3.fromRGB(20, 20, 20) or Color3.fromRGB(8, 8, 8)
	btn.BackgroundTransparency = 0
	btn.BorderSizePixel = 0
	btn.Text = name:upper()
	btn.TextColor3 = (name == currentTab) and Color3.fromRGB(0, 255, 100) or Color3.fromRGB(100, 100, 100)
	btn.Font = Enum.Font.GothamBold
	btn.TextSize = 11
	btn.ZIndex = 6
	btn.LayoutOrder = i
	mkCorner(btn, 10)
	local accent = Instance.new("Frame", btn)
	accent.Name = "Accent"
	accent.Size = UDim2.new(0, 3, 0.6, 0)
	accent.Position = UDim2.new(0, 0, 0.2, 0)
	accent.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
	accent.BorderSizePixel = 0
	accent.Visible = (name == currentTab)
	accent.ZIndex = 7
	mkCorner(accent, 2)
	tabBtns[name] = {btn = btn, accent = accent}
	btn.MouseEnter:Connect(function()
		if name ~= currentTab then
			TweenService:Create(btn, TweenInfo.new(0.1), {TextColor3 = Color3.fromRGB(255, 255, 255), BackgroundColor3 = Color3.fromRGB(12, 12, 12)}):Play()
		end
	end)
	btn.MouseLeave:Connect(function()
		if name ~= currentTab then
			TweenService:Create(btn, TweenInfo.new(0.1), {TextColor3 = Color3.fromRGB(100, 100, 100), BackgroundColor3 = Color3.fromRGB(8, 8, 8)}):Play()
		end
	end)
	btn.MouseButton1Click:Connect(function()
		btn.BackgroundColor3 = C.flashRed
		task.delay(0.08, function() if btn then btn.BackgroundColor3 = Color3.fromRGB(20,20,20) end end)
		currentTab = name
		for _, n in ipairs(TABS) do
			local t = tabBtns[n]; local active = (n == name)
			TweenService:Create(t.btn, TweenInfo.new(0.14), {
				TextColor3 = active and Color3.fromRGB(255,80,80) or Color3.fromRGB(100,100,100),
				BackgroundColor3 = active and Color3.fromRGB(20,20,20) or Color3.fromRGB(8,8,8)
			}):Play()
			t.accent.Visible = active
			if tabPages[n] then tabPages[n].Visible = active end
		end
	end)
end

-- ROW BUILDERS
local currentPage = nil; local lo = 0
local function LO() lo = lo + 1; return lo end
local function makeGap(px)
	local f = Instance.new("Frame", currentPage)
	f.Size = UDim2.new(1, 0, 0, px or 6)
	f.BackgroundTransparency = 1; f.BorderSizePixel = 0; f.LayoutOrder = LO()
end
local function makeSectionHeader(label)
	local wrap = Instance.new("Frame", currentPage)
	wrap.Size = UDim2.new(1, 0, 0, 28)
	wrap.BackgroundTransparency = 1; wrap.BorderSizePixel = 0; wrap.LayoutOrder = LO()
	local lbl = Instance.new("TextLabel", wrap)
	lbl.Size = UDim2.new(1, -24, 1, 0)
	lbl.Position = UDim2.new(0, 12, 0, 0)
	lbl.BackgroundTransparency = 1
	lbl.Text = label and label:upper() or ""
	lbl.TextColor3 = C.sectionTxt
	lbl.Font = Enum.Font.GothamBold
	lbl.TextSize = 10
	lbl.TextXAlignment = Enum.TextXAlignment.Left
end
local function makeInputRow(label, default, onChange)
	local row = Instance.new("Frame", currentPage)
	row.Size = UDim2.new(1, 0, 0, 44)
	row.BackgroundColor3 = C.rowBg
	row.BackgroundTransparency = 1
	row.BorderSizePixel = 0
	row.LayoutOrder = LO()
	local div = Instance.new("Frame", row)
	div.Size = UDim2.new(1, -24, 0, 1)
	div.Position = UDim2.new(0, 12, 1, -1)
	div.BackgroundColor3 = C.rowBorder; div.BorderSizePixel = 0
	local lbl = Instance.new("TextLabel", row)
	lbl.Size = UDim2.new(1, -90, 1, 0)
	lbl.Position = UDim2.new(0, 12, 0, 0)
	lbl.BackgroundTransparency = 1
	lbl.Text = label
	lbl.TextColor3 = C.rowLabel
	lbl.Font = Enum.Font.GothamBold; lbl.TextSize = 13
	lbl.TextXAlignment = Enum.TextXAlignment.Left
	local boxWrap = Instance.new("Frame", row)
	boxWrap.Size = UDim2.new(0, 70, 0, 28)
	boxWrap.Position = UDim2.new(1, -82, 0.5, -14)
	boxWrap.BackgroundColor3 = C.inputBg; boxWrap.BorderSizePixel = 0
	mkCorner(boxWrap, 8)
	local bs = mkStroke(boxWrap, C.inputBorder, 1)
	local box = Instance.new("TextBox", boxWrap)
	box.Size = UDim2.new(1, -8, 1, 0); box.Position = UDim2.new(0, 4, 0, 0)
	box.BackgroundTransparency = 1; box.Text = tostring(default)
	box.TextColor3 = C.inputTxt; box.Font = Enum.Font.GothamBold
	box.TextSize = 13; box.ClearTextOnFocus = false; box.ZIndex = 8
	box.TextXAlignment = Enum.TextXAlignment.Center
	box.Focused:Connect(function() TweenService:Create(bs, TweenInfo.new(0.15), {Color=C.inputFocus}):Play() end)
	box.FocusLost:Connect(function()
		TweenService:Create(bs, TweenInfo.new(0.15), {Color=C.inputBorder}):Play()
		if onChange then local n = tonumber(box.Text); if n then onChange(n) else box.Text = tostring(default) end end
	end)
	return box, row
end

local function makeToggleRow(label, defaultOn, onToggle)
	local row = Instance.new("Frame", currentPage)
	row.Size = UDim2.new(1, 0, 0, 44)
	row.BackgroundTransparency = 1; row.BorderSizePixel = 0; row.LayoutOrder = LO()
	local div = Instance.new("Frame", row)
	div.Size = UDim2.new(1, -24, 0, 1); div.Position = UDim2.new(0, 12, 1, -1)
	div.BackgroundColor3 = C.rowBorder; div.BorderSizePixel = 0
	local lbl = Instance.new("TextLabel", row)
	lbl.Size = UDim2.new(1, -70, 1, 0); lbl.Position = UDim2.new(0, 12, 0, 0)
	lbl.BackgroundTransparency = 1; lbl.Text = label
	lbl.TextColor3 = C.rowLabel; lbl.Font = Enum.Font.GothamBold; lbl.TextSize = 13
	lbl.TextXAlignment = Enum.TextXAlignment.Left
	local pillBg = Instance.new("Frame", row)
	pillBg.Size = UDim2.new(0, 42, 0, 20); pillBg.Position = UDim2.new(1, -54, 0.5, -10)
	pillBg.BackgroundColor3 = defaultOn and C.pillOn or C.pillOff
	pillBg.BorderSizePixel = 0; pillBg.ZIndex = 7
	mkCorner(pillBg, 12); mkStroke(pillBg, C.pillBorder, 1)
	local dot = Instance.new("Frame", pillBg)
	dot.Size = UDim2.new(0, 14, 0, 14)
	dot.Position = defaultOn and UDim2.new(1, -17, 0.5, -7) or UDim2.new(0, 3, 0.5, -7)
	dot.BackgroundColor3 = defaultOn and C.dotOn or C.dotOff
	dot.BorderSizePixel = 0; dot.ZIndex = 8; mkCorner(dot, 7)
	local isOn = defaultOn or false
	
	local function setV(on)
		if on == isOn then return end
		isOn = on
		TweenService:Create(pillBg, TweenInfo.new(0.18, Enum.EasingStyle.Quad), {BackgroundColor3 = on and C.pillOn or C.pillOff}):Play()
		TweenService:Create(dot, TweenInfo.new(0.18, Enum.EasingStyle.Back), { Position = on and UDim2.new(1, -17, 0.5, -7) or UDim2.new(0, 3, 0.5, -7), BackgroundColor3 = on and C.dotOn or C.dotOff }):Play()
		if onToggle then pcall(onToggle, on) end
	end
	
	local function toggle()
		local orig = pillBg.BackgroundColor3
		pillBg.BackgroundColor3 = C.flashRed
		task.delay(0.1, function()
			if pillBg then pillBg.BackgroundColor3 = orig end
		end)
		setV(not isOn)
		task.spawn(function() if saveConfig then pcall(saveConfig) end end)
	end
	local clk = Instance.new("TextButton", row)
	clk.Size = UDim2.new(1, -60, 1, 0); clk.BackgroundTransparency = 1
	clk.Text = ""; clk.ZIndex = 5; clk.BorderSizePixel = 0
	clk.MouseButton1Click:Connect(toggle)
	local pClk = Instance.new("TextButton", pillBg)
	pClk.Size = UDim2.new(1, 0, 1, 0); pClk.BackgroundTransparency = 1
	pClk.Text = ""; pClk.ZIndex = 9; pClk.BorderSizePixel = 0
	pClk.MouseButton1Click:Connect(toggle)
	
	return setV
end

-- KEYBIND ROW
local function getKeyDisplayName(kc)
	local n = kc.Name
	local gpNames = { ButtonA="A",ButtonB="B",ButtonX="X",ButtonY="Y", ButtonL1="LB",ButtonL2="LT",ButtonL3="LS", ButtonR1="RB",ButtonR2="RT",ButtonR3="RS", ButtonSelect="SEL",ButtonStart="STA", DPadUp="D↑",DPadDown="D↓",DPadLeft="D←",DPadRight="D→", Thumbstick1="LS",Thumbstick2="RS", }
	if gpNames[n] then return gpNames[n] end
	return n:sub(1, 5)
end
local function makeKeybindRow(label, currentKey, onChanged, keyName)
	local row = Instance.new("Frame", currentPage)
	row.Size = UDim2.new(1, 0, 0, 44)
	row.BackgroundTransparency = 1; row.BorderSizePixel = 0; row.LayoutOrder = LO()
	local div = Instance.new("Frame", row)
	div.Size = UDim2.new(1, -24, 0, 1); div.Position = UDim2.new(0, 12, 1, -1)
	div.BackgroundColor3 = C.rowBorder; div.BorderSizePixel = 0
	local lbl = Instance.new("TextLabel", row)
	lbl.Size = UDim2.new(1, -75, 1, 0); lbl.Position = UDim2.new(0, 12, 0, 0)
	lbl.BackgroundTransparency = 1; lbl.Text = label
	lbl.TextColor3 = C.rowLabel; lbl.Font = Enum.Font.GothamBold; lbl.TextSize = 13
	lbl.TextXAlignment = Enum.TextXAlignment.Left
	local kbtn = Instance.new("TextButton", row)
	kbtn.Size = UDim2.new(0, 52, 0, 28); kbtn.Position = UDim2.new(1, -64, 0.5, -14)
	kbtn.BackgroundColor3 = C.chipBg; kbtn.BorderSizePixel = 0
	kbtn.Text = getKeyDisplayName(currentKey); kbtn.TextColor3 = C.chipTxt
	kbtn.Font = Enum.Font.GothamBold; kbtn.TextSize = 10; kbtn.ZIndex = 8
	mkCorner(kbtn, 10); local ks = mkStroke(kbtn, C.chipBorder, 1)
	local listening = false; local lconnKeyboard = nil; local lconnGamepad = nil
	local function stopL(key)
		listening = false
		if lconnKeyboard then lconnKeyboard:Disconnect(); lconnKeyboard = nil end
		if lconnGamepad then lconnGamepad:Disconnect(); lconnGamepad = nil end
		TweenService:Create(ks, TweenInfo.new(0.12), {Color=C.chipBorder}):Play()
		kbtn.TextColor3 = C.chipTxt
		if key then
			kbtn.Text = getKeyDisplayName(key)
			if onChanged then onChanged(key) end
			task.spawn(function() if saveConfig then pcall(saveConfig) end end)
		end
	end
	kbtn.MouseButton1Click:Connect(function()
		local orig = kbtn.BackgroundColor3
		kbtn.BackgroundColor3 = C.flashRed
		task.delay(0.1, function()
			if kbtn then kbtn.BackgroundColor3 = orig end
		end)
		if listening then stopL(nil); return end
		listening = true; kbtn.Text = "..."; kbtn.TextColor3 = C.inputTxt
		TweenService:Create(ks, TweenInfo.new(0.12), {Color=C.inputFocus}):Play()
		lconnKeyboard = UIS.InputBegan:Connect(function(inp)
			if not listening then return end
			if inp.UserInputType ~= Enum.UserInputType.Keyboard then return end
			if inp.KeyCode == Enum.KeyCode.Escape then stopL(nil); return end
			stopL(inp.KeyCode)
		end)
		lconnGamepad = UIS.InputBegan:Connect(function(inp)
			if not listening then return end
			if inp.UserInputType ~= Enum.UserInputType.Gamepad1 and inp.UserInputType ~= Enum.UserInputType.Gamepad2 and inp.UserInputType ~= Enum.UserInputType.Gamepad3 and inp.UserInputType ~= Enum.UserInputType.Gamepad4 then return end
			local kc = inp.KeyCode; if kc == Enum.KeyCode.Unknown then return end
			stopL(kc)
		end)
	end)
	if keyName then keybindBtnRefs[keyName] = kbtn end
	return kbtn
end

-- BUILD PAGES
local function buildPage(tabName, buildFn)
	local page = Instance.new("ScrollingFrame", contentBg)
	page.Name = tabName; page.Visible = (tabName == "Speed")
	page.Size = UDim2.new(1, 0, 1, 0); page.Position = UDim2.new(0, 0, 0, 0)
	page.BackgroundTransparency = 1; page.BorderSizePixel = 0
	page.ScrollBarThickness = 3
	page.ScrollBarImageColor3 = C.accent
	page.ScrollBarImageTransparency = 0.4
	page.AutomaticCanvasSize = Enum.AutomaticSize.Y
	page.CanvasSize = UDim2.new(0, 0, 0, 0)
	local ll = Instance.new("UIListLayout", page)
	ll.SortOrder = Enum.SortOrder.LayoutOrder; ll.Padding = UDim.new(0, 0)
	tabPages[tabName] = page; currentPage = page; lo = 0
	buildFn()
	currentPage = nil
end

-- SPEED PAGE
buildPage("Speed", function()
	makeGap(2)
	makeSectionHeader("Speed Settings")
	makeGap(2)
	normalBox = makeInputRow("Normal Speed", State.normalSpeed, function(n) if n > 0 and n <= 500 then State.normalSpeed = n end end)
	carryBox = makeInputRow("Carry Speed", State.carrySpeed, function(n) if n > 0 and n <= 500 then State.carrySpeed = n end end)
	laggerBox = makeInputRow("Lagger Speed", State.laggerSpeed, function(n) if n > 0 and n <= 500 then State.laggerSpeed = n end end)
	makeGap(4)
	makeSectionHeader("PRC Performance")
	makeGap(2)
	local prcRow = Instance.new("Frame", currentPage)
	prcRow.Size = UDim2.new(1, 0, 0, 44)
	prcRow.BackgroundTransparency = 1; prcRow.BorderSizePixel = 0; prcRow.LayoutOrder = LO()
	local prcWrap = Instance.new("Frame", prcRow)
	prcWrap.Size = UDim2.new(1, -24, 0, 34)
	prcWrap.Position = UDim2.new(0, 12, 0, 5)
	prcWrap.BackgroundColor3 = C.modeBtnBg; prcWrap.BorderSizePixel = 0
	mkCorner(prcWrap, 8); mkStroke(prcWrap, C.modeBtnBrd, 1)
	local prcLL = Instance.new("UIListLayout", prcWrap)
	prcLL.FillDirection = Enum.FillDirection.Horizontal
	prcLL.SortOrder = Enum.SortOrder.LayoutOrder; prcLL.Padding = UDim.new(0, 0)
	local prcModes = {"Default", "Normal", "High"}
	local prcBtns = {}
	for i, mode in ipairs(prcModes) do
		local b = Instance.new("TextButton", prcWrap)
		b.Size = UDim2.new(1/3, 0, 1, 0)
		b.BackgroundColor3 = (mode == State.prcMode) and C.modeBtnActBg or Color3.fromRGB(0,0,0)
		b.BackgroundTransparency = (mode == State.prcMode) and 0 or 1
		b.BorderSizePixel = 0; b.Text = mode
		b.TextColor3 = (mode == State.prcMode) and C.modeBtnActTx or C.modeBtnTxt
		b.Font = Enum.Font.GothamBold; b.TextSize = 12; b.ZIndex = 8
		b.LayoutOrder = i; mkCorner(b, 8)
		b.MouseButton1Click:Connect(function()
			local orig = b.BackgroundColor3
			b.BackgroundColor3 = C.flashRed
			task.delay(0.1, function() if b then b.BackgroundColor3 = orig end end)
			for _, m in ipairs(prcModes) do
				local btn = prcBtns[m]
				if btn then
					TweenService:Create(btn, TweenInfo.new(0.15), { BackgroundColor3 = (m == mode) and C.modeBtnActBg or Color3.fromRGB(0,0,0), BackgroundTransparency = (m == mode) and 0 or 1, TextColor3 = (m == mode) and C.modeBtnActTx or C.modeBtnTxt, }):Play()
				end
			end
			applyPRCMode(mode)
		end)
		prcBtns[mode] = b
	end
	local prcInfo = Instance.new("TextLabel", currentPage)
	prcInfo.Size = UDim2.new(1, -24, 0, 20)
	prcInfo.Position = UDim2.new(0, 12, 0, 0)
	prcInfo.BackgroundTransparency = 1
	prcInfo.Text = "Default: 60 FPS | Normal: 120 FPS | High: Unlimited + Boost"
	prcInfo.TextColor3 = C.rowSub
	prcInfo.Font = Enum.Font.Gotham
	prcInfo.TextSize = 9
	prcInfo.TextXAlignment = Enum.TextXAlignment.Left
	prcInfo.LayoutOrder = LO()
	makeGap(4)
	local modeRow = Instance.new("Frame", currentPage)
	modeRow.Size = UDim2.new(1, 0, 0, 48)
	modeRow.BackgroundTransparency = 1; modeRow.BorderSizePixel = 0; modeRow.LayoutOrder = LO()
	local modeWrap = Instance.new("Frame", modeRow)
	modeWrap.Size = UDim2.new(1, -24, 0, 34)
	modeWrap.Position = UDim2.new(0, 12, 0, 7)
	modeWrap.BackgroundColor3 = C.modeBtnBg; modeWrap.BorderSizePixel = 0
	mkCorner(modeWrap, 8); mkStroke(modeWrap, C.modeBtnBrd, 1)
	local modeLL = Instance.new("UIListLayout", modeWrap)
	modeLL.FillDirection = Enum.FillDirection.Horizontal
	modeLL.SortOrder = Enum.SortOrder.LayoutOrder; modeLL.Padding = UDim.new(0, 0)
	local modeStatusRow = Instance.new("Frame", currentPage)
	modeStatusRow.Size = UDim2.new(1, 0, 0, 22)
	modeStatusRow.BackgroundTransparency = 1; modeStatusRow.BorderSizePixel = 0; modeStatusRow.LayoutOrder = LO()
	local modeStatusLbl = Instance.new("TextLabel", modeStatusRow)
	modeStatusLbl.Size = UDim2.new(1, -24, 1, 0); modeStatusLbl.Position = UDim2.new(0, 12, 0, 0)
	modeStatusLbl.BackgroundTransparency = 1; modeStatusLbl.Text = "Mode: Normal"
	modeStatusLbl.TextColor3 = C.rowSub; modeStatusLbl.Font = Enum.Font.Gotham
	modeStatusLbl.TextSize = 11; modeStatusLbl.TextXAlignment = Enum.TextXAlignment.Left
	local modeNames = {"Normal", "Carry", "Lagger"}
	local modeBtns = {}
	local function setModeActive(active)
		for _, m in ipairs(modeNames) do
			local b = modeBtns[m]; if not b then continue end
			local isActive = (m == active)
			TweenService:Create(b, TweenInfo.new(0.15), { BackgroundColor3 = isActive and C.modeBtnActBg or Color3.fromRGB(0,0,0), BackgroundTransparency = isActive and 0 or 1, TextColor3 = isActive and C.modeBtnActTx or C.modeBtnTxt, }):Play()
		end
		modeStatusLbl.Text = "Mode: " .. active
		if active == "Normal" then
			State.speedToggled = false; State.laggerEnabled = false
			if stackBtnRefs.carrySpeed then stackBtnRefs.carrySpeed.setOn(false) end
			if stackBtnRefs.lagger then stackBtnRefs.lagger.setOn(false) end
		elseif active == "Carry" then
			State.speedToggled = true; State.laggerEnabled = false
			if stackBtnRefs.carrySpeed then stackBtnRefs.carrySpeed.setOn(true) end
			if stackBtnRefs.lagger then stackBtnRefs.lagger.setOn(false) end
		elseif active == "Lagger" then
			State.speedToggled = false; State.laggerEnabled = true
			if stackBtnRefs.carrySpeed then stackBtnRefs.carrySpeed.setOn(false) end
			if stackBtnRefs.lagger then stackBtnRefs.lagger.setOn(true) end
		end
	end
	for i, mname in ipairs(modeNames) do
		local b = Instance.new("TextButton", modeWrap)
		b.Size = UDim2.new(1/3, 0, 1, 0)
		b.BackgroundColor3 = (i == 1) and C.modeBtnActBg or Color3.fromRGB(0,0,0)
		b.BackgroundTransparency = (i == 1) and 0 or 1
		b.BorderSizePixel = 0; b.Text = mname
		b.TextColor3 = (i == 1) and C.modeBtnActTx or C.modeBtnTxt
		b.Font = Enum.Font.GothamBold; b.TextSize = 12; b.ZIndex = 8
		b.LayoutOrder = i; mkCorner(b, 8)
		b.MouseButton1Click:Connect(function()
			local orig = b.BackgroundColor3
			b.BackgroundColor3 = C.flashRed
			task.delay(0.1, function() if b then b.BackgroundColor3 = orig end end)
			setModeActive(mname)
		end)
		modeBtns[mname] = b
	end
end)

-- MECHANICS PAGE (Mega Auto Grab only)
buildPage("Mech", function()
	makeGap(2)
	makeSectionHeader("Mega Auto Grab")
	makeGap(2)
	
	-- Toggle
	setInstaGrab = makeToggleRow("Auto Grab", Steal.AutoStealEnabled, function(on)
		Steal.AutoStealEnabled = on
		if on then
			startAutoSteal()
		else
			stopAutoSteal()
		end
		task.spawn(function() if saveConfig then pcall(saveConfig) end end)
	end)
	
	-- Radius input
	stealRadBox = makeInputRow("Grab Radius", Steal.StealRadius, function(n)
		if n >= 5 and n <= 300 then
			Steal.StealRadius = math.floor(n)
			if radTB and not radTB:IsFocused() then radTB.Text = tostring(Steal.StealRadius) end
			if radLbl then radLbl.Text = "R: " .. tostring(Steal.StealRadius) end
			task.spawn(function() if saveConfig then pcall(saveConfig) end end)
		end
	end)
	
	-- Duration input
	makeInputRow("Grab Duration (0.5-4)", Steal.StealDuration, function(n)
		if n >= 0.5 and n <= 4 then
			Steal.StealDuration = n
			task.spawn(function() if saveConfig then pcall(saveConfig) end end)
		end
	end)
	
	makeGap(8)
	makeSectionHeader("Combat / Defense")
	makeGap(2)
	setInfJump = makeToggleRow("Infinite Jump", false, function(on) State.infJumpEnabled = on end)
	local jumpPowerBox = makeInputRow("Jump Power (1-200)", State.jumpPower, function(n)
		if n and n >= 1 and n <= 200 then
			State.jumpPower = n
		end
	end)
	setAntiRag = makeToggleRow("Anti Ragdoll", false, function(on) State.antiRagdollEnabled = on; if on then startAntiRagdoll() else stopAntiRagdoll() end end)
	setFps = makeToggleRow("FPS Boost", false, function(on) State.fpsBoostEnabled = on; if on then pcall(applyFPSBoost) end end)
	setMedusaCounter = makeToggleRow("Medusa Counter", false, function(on) State.medusaCounterEnabled = on; if on then setupMedusaCounter(LP.Character) else stopMedusaCounter() end end)
	setUnwalkToggle = makeToggleRow("Unwalk", false, function(on)
		State.unwalkEnabled = on
		if on then
			startUnwalk()
		else
			stopUnwalk()
		end
	end)
	makeGap(4)
	makeSectionHeader("ESP")
	makeGap(2)
	local playerEspSetter = makeToggleRow("Player ESP", State.playerEsp, function(on)
		State.playerEsp = on
		if on then startESP() else stopESP() end
	end)
	_G.toggleSetters = _G.toggleSetters or {}
	_G.toggleSetters.playerEsp = playerEspSetter

	-- ANIMATION PACKS
	makeGap(8)
	makeSectionHeader("Animation Pack")
	makeGap(2)

	local animPackActive = nil
	local animPackOriginal = nil
	local animPackSetters = {}

	local ZOMBIE_ANIMS = {
		idle1    = "rbxassetid://10921344533",
		idle2    = "rbxassetid://10921345304",
		walk     = "rbxassetid://10921355261",
		run      = "rbxassetid://616163682",
		jump     = "rbxassetid://10921351278",
		fall     = "rbxassetid://10921350320",
		climb    = "rbxassetid://10921343576",
		swim     = "rbxassetid://10921352344",
		swimidle = "rbxassetid://10921353442",
	}
	local ELDER_ANIMS = {
		idle1    = "rbxassetid://10921101664",
		idle2    = "rbxassetid://10921102574",
		walk     = "rbxassetid://10921111375",
		run      = "rbxassetid://10921104374",
		jump     = "rbxassetid://10921107367",
		fall     = "rbxassetid://10921105765",
		climb    = "rbxassetid://10921100400",
		swim     = "rbxassetid://10921108971",
		swimidle = "rbxassetid://10921110146",
	}

	local function saveAnimOriginal(char)
		local animate = char:FindFirstChild("Animate")
		if not animate then return nil end
		local function g(obj) return obj and obj.AnimationId or nil end
		return {
			idle1    = g(animate.idle     and animate.idle.Animation1),
			idle2    = g(animate.idle     and animate.idle.Animation2),
			walk     = g(animate.walk     and animate.walk.WalkAnim),
			run      = g(animate.run      and animate.run.RunAnim),
			jump     = g(animate.jump     and animate.jump.JumpAnim),
			fall     = g(animate.fall     and animate.fall.FallAnim),
			climb    = g(animate.climb    and animate.climb.ClimbAnim),
			swim     = g(animate.swim     and animate.swim.Swim),
			swimidle = g(animate.swimidle and animate.swimidle.SwimIdle),
		}
	end

	local function applyAnimIds(char, ids)
		local animate = char:FindFirstChild("Animate")
		if not animate then return end
		local function s(obj, id) if obj and id then obj.AnimationId = id end end
		s(animate.idle     and animate.idle.Animation1,     ids.idle1)
		s(animate.idle     and animate.idle.Animation2,     ids.idle2)
		s(animate.walk     and animate.walk.WalkAnim,       ids.walk)
		s(animate.run      and animate.run.RunAnim,         ids.run)
		s(animate.jump     and animate.jump.JumpAnim,       ids.jump)
		s(animate.fall     and animate.fall.FallAnim,       ids.fall)
		s(animate.climb    and animate.climb.ClimbAnim,     ids.climb)
		s(animate.swim     and animate.swim.Swim,           ids.swim)
		s(animate.swimidle and animate.swimidle.SwimIdle,   ids.swimidle)
		local hum = char:FindFirstChildOfClass("Humanoid")
		if hum then
			pcall(function()
				for _, track in ipairs(hum:GetPlayingAnimationTracks()) do track:Stop(0) end
				hum:ChangeState(Enum.HumanoidStateType.Running)
			end)
		end
	end

	local function restoreAnimOriginal(char)
		if not animPackOriginal then return end
		applyAnimIds(char, animPackOriginal)
		animPackOriginal = nil
	end

	local function enableAnimPack(packName, ids)
		local char = LP.Character
		if not char then return end
		if not animPackOriginal then
			animPackOriginal = saveAnimOriginal(char)
		end
		animPackActive = packName
		_G._nightmareAnimPackActive = packName
		applyAnimIds(char, ids)
	end

	local function disableAnimPack()
		animPackActive = nil
		_G._nightmareAnimPackActive = nil
		local char = LP.Character
		if char then restoreAnimOriginal(char) end
		for _, setter in pairs(animPackSetters) do
			pcall(function() setter(false) end)
		end
	end

	local zombieAnimSetter = makeToggleRow("Zombie Animation", false, function(on)
		if on then
			if animPackActive == "Elder" and animPackSetters["Elder"] then
				animPackSetters["Elder"](false)
			end
			enableAnimPack("Zombie", ZOMBIE_ANIMS)
		else
			if animPackActive == "Zombie" then
				disableAnimPack()
			end
		end
	end)
	animPackSetters["Zombie"] = zombieAnimSetter

	local elderAnimSetter = makeToggleRow("Elder Animation", false, function(on)
		if on then
			if animPackActive == "Zombie" and animPackSetters["Zombie"] then
				animPackSetters["Zombie"](false)
			end
			enableAnimPack("Elder", ELDER_ANIMS)
		else
			if animPackActive == "Elder" then
				disableAnimPack()
			end
		end
	end)
	animPackSetters["Elder"] = elderAnimSetter

	local WICKED_POPULAR_ANIMS = {
		idle1    = "rbxassetid://118832222982049",
		idle2    = "rbxassetid://76049494037641",
		walk     = "rbxassetid://92072849924640",
		run      = "rbxassetid://72301599441680",
		jump     = "rbxassetid://104325245285198",
		fall     = "rbxassetid://121152442762481",
		climb    = "rbxassetid://131326830509784",
		swim     = "rbxassetid://99384245425157",
		swimidle = "rbxassetid://113199415118199",
	}
	local wickedPopularSetter = makeToggleRow("Wicked Popular Anim", false, function(on)
		if on then
			if animPackActive and animPackSetters[animPackActive] then
				animPackSetters[animPackActive](false)
			end
			enableAnimPack("WickedPopular", WICKED_POPULAR_ANIMS)
		else
			if animPackActive == "WickedPopular" then
				disableAnimPack()
			end
		end
	end)
	animPackSetters["WickedPopular"] = wickedPopularSetter

	_G._nightmareAnimPackSetters = {
		Zombie       = zombieAnimSetter,
		Elder        = elderAnimSetter,
		WickedPopular = wickedPopularSetter,
	}
end)

-- KEYBIND PAGE
buildPage("Keybind", function()
	makeGap(10)
	makeSectionHeader("Keybinds")
	makeGap(2)
	makeKeybindRow("Speed / Carry", Keys.speed, function(k) Keys.speed = k end, "speed")
	makeKeybindRow("Auto Left", Keys.autoLeft, function(k) Keys.autoLeft = k end, "autoLeft")
	makeKeybindRow("Auto Right", Keys.autoRight, function(k) Keys.autoRight = k end, "autoRight")
	makeKeybindRow("Lagger Mode", Keys.lagger, function(k) Keys.lagger = k end, "lagger")
	makeKeybindRow("Aimbot", Keys.aimbot, function(k) Keys.aimbot = k end, "aimbot")
	makeKeybindRow("Drop Brainrot", Keys.drop, function(k) Keys.drop = k end, "drop")
	makeKeybindRow("TP Down", Keys.tpDown, function(k) Keys.tpDown = k end, "tpDown")
	makeGap(8)
	makeSectionHeader("UI Controls")
	makeGap(2)
	makeKeybindRow("Hide / Show GUI", Keys.guiHide, function(k) Keys.guiHide = k end, "guiHide")
end)

-- AIMBOT PAGE (Envy Bat Aimbot settings)
buildPage("Aimbot", function()
	makeGap(2)
	makeSectionHeader("Envy Bat Aimbot")
	makeGap(2)
	
	-- Aimbot toggle
	setBatAimbot = makeToggleRow("Bat Aimbot", EnvyAimbot.enabled, function(on)
		setEnvyAimbotState(on)
		if stackBtnRefs.aimbot then stackBtnRefs.aimbot.setOn(on) end
		task.spawn(function() if saveConfig then pcall(saveConfig) end end)
	end)
	
	makeGap(4)
	makeSectionHeader("Movement Parameters")
	makeGap(2)
	
	makeInputRow("Chase Speed (10-150)", EnvyAimbot.speed, function(n)
		if n >= 10 and n <= 150 then EnvyAimbot.speed = n end
	end)
	
	makeInputRow("Vertical Speed (10-150)", EnvyAimbot.vertSpeed, function(n)
		if n >= 10 and n <= 150 then EnvyAimbot.vertSpeed = n end
	end)
	
	makeInputRow("Distance from Target (-10 to 5)", EnvyAimbot.distance, function(n)
		if n >= -10 and n <= 5 then EnvyAimbot.distance = n end
	end)
	
	makeInputRow("Height Offset (1-10)", EnvyAimbot.height, function(n)
		if n >= 1 and n <= 10 then EnvyAimbot.height = n end
	end)
	
	makeInputRow("Vertical Aim Offset (0-5)", EnvyAimbot.vertOffset, function(n)
		if n >= 0 and n <= 5 then EnvyAimbot.vertOffset = n end
	end)
	
	makeGap(4)
	makeSectionHeader("Rotation & Swing")
	makeGap(2)
	
	makeInputRow("Turn Speed (50-600)", EnvyAimbot.turnSpeed, function(n)
		if n >= 50 and n <= 600 then EnvyAimbot.turnSpeed = n end
	end)
	
	makeInputRow("Max Turn Rate (10-80)", EnvyAimbot.maxTurnRate, function(n)
		if n >= 10 and n <= 80 then EnvyAimbot.maxTurnRate = n end
	end)
	
	makeToggleRow("Swing on Approach", EnvyAimbot.swingEnabled, function(on)
		EnvyAimbot.swingEnabled = on
		task.spawn(function() if saveConfig then pcall(saveConfig) end end)
	end)

	makeGap(8)
	makeSectionHeader("Bat Counter")
	makeGap(2)
	
	setBatCounter = makeToggleRow("Bat Counter", false, function(on)
		State.batCounterEnabled = on
		if on then
			startBatCounter()
		else
			stopBatCounter()
		end
	end)

	makeGap(8)
	makeSectionHeader("Dark Angel Bypasser")
	makeGap(2)

	setBypassToggle = makeToggleRow("Dark Angel Bypasser", bypassActivated, function(on)
		bypassActivated = on
		if on then
			startBypassLag()
		else
			stopBypassLag()
			bypassActivated = false
		end
		task.spawn(function() if saveConfig then pcall(saveConfig) end end)
	end)

	local _pwrBox, _ = makeInputRow("Power (10k-500k)", bypassPower, function(n)
		applyBypassPower(n)
		task.spawn(function() if saveConfig then pcall(saveConfig) end end)
	end)
	bypassPowerBox = _pwrBox

	makeGap(8)
	makeSectionHeader("Auto TP Down")
	makeGap(2)

	makeToggleRow("Auto TP Down", State.autoTpDownEnabled, function(on)
		State.autoTpDownEnabled = on
		task.spawn(function() if saveConfig then pcall(saveConfig) end end)
	end)

	makeInputRow("Y Trigger", State.autoTpDownY, function(v)
		local n = tonumber(v)
		if n then State.autoTpDownY = n; task.spawn(function() if saveConfig then pcall(saveConfig) end end) end
	end)
end)

-- DARK ANGEL VISUAL PAGE
buildPage("Dark Angel Visual", function()
	makeGap(2)
	makeSectionHeader("Auto Movement")
	makeGap(2)
	makeGap(8)
	makeSectionHeader("Cursed Booster")
	makeGap(2)

	local stretchRezSetter = makeToggleRow("Stretch Rez", State.stretchRez, function(on)
		State.stretchRez = on
		pcall(function()
			local cam = workspace.CurrentCamera
			if cam then cam.FieldOfView = on and 120 or (currentFOV or 70) end
		end)
	end)
	_G.toggleSetters = _G.toggleSetters or {}
	_G.toggleSetters.stretchRez = stretchRezSetter

	local nightTimeSetter = makeToggleRow("Night Time", State.nightTime, function(on)
		State.nightTime = on
		pcall(function()
			local L = game:GetService("Lighting")
			if on then
				L.ClockTime = 0
				L.Brightness = 0.2
				L.Ambient = Color3.fromRGB(20, 20, 35)
			else
				L.ClockTime = 14
				L.Brightness = 2
				L.Ambient = Color3.fromRGB(128, 128, 128)
			end
		end)
	end)
	_G.toggleSetters.nightTime = nightTimeSetter

	local purpleSkySetter = makeToggleRow("Purple Sky (Cursed)", State.purpleSky, function(on)
		State.purpleSky = on
		pcall(function()
			local L = game:GetService("Lighting")
			if on then
				L.Ambient = Color3.fromRGB(60, 30, 90)
				L.FogColor = Color3.fromRGB(80, 40, 120)
				local sky = L:FindFirstChildOfClass("Sky") or Instance.new("Sky", L)
				sky.SkyboxBk = "rbxassetid://1534951537"
				sky.SkyboxDn = "rbxassetid://1534951537"
				sky.SkyboxFt = "rbxassetid://1534951537"
				sky.SkyboxLf = "rbxassetid://1534951537"
				sky.SkyboxRt = "rbxassetid://1534951537"
				sky.SkyboxUp = "rbxassetid://1534951537"
				sky.StarCount = 8000
				local cc = L:FindFirstChildOfClass("ColorCorrectionEffect") or Instance.new("ColorCorrectionEffect", L)
				cc.TintColor = Color3.fromRGB(180, 140, 255)
				cc.Saturation = 0.6
				cc.Contrast = 0.15
			else
				L.Ambient = Color3.fromRGB(128, 128, 128)
				L.FogColor = Color3.fromRGB(192, 192, 192)
				local sky = L:FindFirstChildOfClass("Sky")
				if sky then sky:Destroy() end
				local cc = L:FindFirstChildOfClass("ColorCorrectionEffect")
				if cc then cc:Destroy() end
			end
		end)
	end)
	_G.toggleSetters.purpleSky = purpleSkySetter

	local fpsBoosterSetter = makeToggleRow("FPS Booster (Cursed)", State.fpsBoosterCursed, function(on)
		State.fpsBoosterCursed = on
		pcall(function()
			if on then
				settings().Rendering.QualityLevel = Enum.QualityLevel.Level01
				local L = game:GetService("Lighting")
				L.GlobalShadows = false
				L.Brightness = 3
				L.FogEnd = 9e9
				L.Technology = Enum.Technology.Legacy
				L.EnvironmentDiffuseScale = 0
				L.EnvironmentSpecularScale = 0
			else
				settings().Rendering.QualityLevel = Enum.QualityLevel.Automatic
				local L = game:GetService("Lighting")
				L.GlobalShadows = true
				L.Brightness = 2
				L.FogEnd = 100000
				L.Technology = Enum.Technology.ShadowMap
			end
		end)
	end)
	_G.toggleSetters.fpsBoosterCursed = fpsBoosterSetter

	local antiLagSetter = makeToggleRow("Anti-Lag (Clean)", State.antiLagClean, function(on)
		State.antiLagClean = on
		if on then
			task.spawn(function()
				pcall(function()
					for _, obj in ipairs(workspace:GetDescendants()) do
						if obj:IsA("ParticleEmitter") or obj:IsA("PointLight") or obj:IsA("SpotLight") or obj:IsA("Beam") or obj:IsA("Fire") then
							obj:Destroy()
						end
					end
				end)
			end)
		end
		task.spawn(function() if saveConfig then pcall(saveConfig) end end)
	end)
	_G.toggleSetters = _G.toggleSetters or {}
	_G.toggleSetters.antiLagClean = antiLagSetter

	local cloudyTimeSetter = makeToggleRow("Cloudy Time", State.cloudyTime, function(on)
		State.cloudyTime = on
		pcall(function()
			local L = game:GetService("Lighting")
			if on then
				L.ClockTime = 7
				L.Brightness = 1.2
				L.Ambient = Color3.fromRGB(210, 185, 195)
				L.OutdoorAmbient = Color3.fromRGB(220, 195, 205)
				L.FogEnd = 800
				L.FogColor = Color3.fromRGB(230, 210, 215)
				local sky = L:FindFirstChildOfClass("Sky") or Instance.new("Sky", L)
				sky.SkyboxBk = "rbxassetid://159454668"
				sky.SkyboxDn = "rbxassetid://159454667"
				sky.SkyboxFt = "rbxassetid://159454669"
				sky.SkyboxLf = "rbxassetid://159454670"
				sky.SkyboxRt = "rbxassetid://159454671"
				sky.SkyboxUp = "rbxassetid://159454672"
				local existCC = L:FindFirstChildOfClass("ColorCorrectionEffect")
				if existCC then existCC:Destroy() end
				local cc = Instance.new("ColorCorrectionEffect", L)
				cc.TintColor = Color3.fromRGB(255, 220, 225)
				cc.Saturation = 0.08
				cc.Brightness = 0.04
				cc.Contrast = 0.05
			else
				L.ClockTime = 14
				L.Brightness = 2
				L.Ambient = Color3.fromRGB(128, 128, 128)
				L.OutdoorAmbient = Color3.fromRGB(128, 128, 128)
				L.FogEnd = 100000
				L.FogColor = Color3.fromRGB(192, 192, 192)
				local sky = L:FindFirstChildOfClass("Sky")
				if sky then sky:Destroy() end
				local cc = L:FindFirstChildOfClass("ColorCorrectionEffect")
				if cc then cc:Destroy() end
			end
		end)
		task.spawn(function() if saveConfig then pcall(saveConfig) end end)
	end)
	_G.toggleSetters.cloudyTime = cloudyTimeSetter

	local darkModeSetter = makeToggleRow("Dark Mode", State.darkModeEnabled or false, function(on)
		State.darkModeEnabled = on
		pcall(function()
			local L = game:GetService("Lighting")
			if on then
				L.ClockTime = 20
				L.Brightness = 1.2
				L.Ambient = Color3.fromRGB(80, 85, 105)
				L.OutdoorAmbient = Color3.fromRGB(90, 95, 115)
				L.FogEnd = 100000
				L.FogColor = Color3.fromRGB(60, 65, 80)
				local existCC = L:FindFirstChildOfClass("ColorCorrectionEffect")
				if existCC then existCC:Destroy() end
				local cc = Instance.new("ColorCorrectionEffect", L)
				cc.TintColor = Color3.fromRGB(200, 205, 230)
				cc.Saturation = -0.1
				cc.Brightness = -0.02
				cc.Contrast = 0.03
			else
				L.ClockTime = 14
				L.Brightness = 2
				L.Ambient = Color3.fromRGB(128, 128, 128)
				L.OutdoorAmbient = Color3.fromRGB(128, 128, 128)
				L.FogEnd = 100000
				L.FogColor = Color3.fromRGB(192, 192, 192)
				local cc = L:FindFirstChildOfClass("ColorCorrectionEffect")
				if cc then cc:Destroy() end
			end
		end)
		task.spawn(function() if saveConfig then pcall(saveConfig) end end)
	end)
	_G.toggleSetters = _G.toggleSetters or {}
	_G.toggleSetters.darkModeEnabled = darkModeSetter

	local optimizerSetter = makeToggleRow("Optimizer", State.optimizerEnabled or false, function(on)
		State.optimizerEnabled = on
		pcall(function()
			if on then
				pcall(function() settings().Rendering.QualityLevel = Enum.QualityLevel.Level01 end)
				local L = game:GetService("Lighting")
				L.GlobalShadows = false
				L.FogEnd = 9e9
				L.Brightness = 2
				for _, obj in ipairs(workspace:GetDescendants()) do
					pcall(function()
						if obj:IsA("ParticleEmitter") or obj:IsA("Smoke") or obj:IsA("Fire") or obj:IsA("Sparkles") then
							obj.Enabled = false
						end
					end)
				end
			else
				pcall(function() settings().Rendering.QualityLevel = Enum.QualityLevel.Automatic end)
				local L = game:GetService("Lighting")
				L.GlobalShadows = true
				L.FogEnd = 100000
				L.Brightness = 2
			end
		end)
		task.spawn(function() if saveConfig then pcall(saveConfig) end end)
	end)
	_G.toggleSetters = _G.toggleSetters or {}
	_G.toggleSetters.optimizerEnabled = optimizerSetter

	local xraySetter = makeToggleRow("X-Ray", State.xrayEnabled or false, function(on)
		State.xrayEnabled = on
		pcall(function()
			for _, player in ipairs(Players:GetPlayers()) do
				if player ~= LP then
					local char = player.Character
					if char then
						if on then
							for _, part in ipairs(char:GetDescendants()) do
								if part:IsA("BasePart") then
									pcall(function() part.CastShadow = false end)
									if not char:FindFirstChild("DARK ANGELHUB_XRAY_"..part.Name) then
										local box = Instance.new("SelectionBox")
										box.Name = "DARK ANGELHUB_XRAY_"..part.Name
										box.Color3 = Color3.fromRGB(255, 255, 255)
										box.SurfaceColor3 = Color3.fromRGB(140, 0, 0)
										box.SurfaceTransparency = 0.5
										box.LineThickness = 0.04
										box.Adornee = part
										box.Parent = char
									end
								end
							end
						else
							for _, v in ipairs(char:GetChildren()) do
								if v.Name:sub(1, 16) == "DARK ANGELHUB_XRAY_" then pcall(function() v:Destroy() end) end
							end
						end
					end
				end
			end
		end)
		task.spawn(function() if saveConfig then pcall(saveConfig) end end)
	end)
	_G.toggleSetters.xrayEnabled = xraySetter

	makeGap(8)
	makeSectionHeader("Jump Mode")
	makeGap(4)
	State.infJumpMode = State.infJumpMode or "manual"
	local jumpModeContainer = Instance.new("Frame", currentPage)
	jumpModeContainer.Size = UDim2.new(1,0,0,36)
	jumpModeContainer.BackgroundTransparency = 1
	jumpModeContainer.BorderSizePixel = 0
	jumpModeContainer.LayoutOrder = LO()
	local manuelBtn = Instance.new("TextButton", jumpModeContainer)
	manuelBtn.Size = UDim2.new(0.5,-3,1,0)
	manuelBtn.Position = UDim2.new(0,0,0,0)
	manuelBtn.BackgroundColor3 = C.modeBtnActBg
	manuelBtn.BorderSizePixel = 0
	manuelBtn.Text = "Manual"
	manuelBtn.TextColor3 = C.modeBtnActTx
	manuelBtn.Font = Enum.Font.GothamBold
	manuelBtn.TextSize = 12
	manuelBtn.AutoButtonColor = false
	mkCorner(manuelBtn, 8)
	local holdBtn = Instance.new("TextButton", jumpModeContainer)
	holdBtn.Size = UDim2.new(0.5,-3,1,0)
	holdBtn.Position = UDim2.new(0.5,3,0,0)
	holdBtn.BackgroundColor3 = Color3.fromRGB(0,0,0)
	holdBtn.BackgroundTransparency = 1
	holdBtn.BorderSizePixel = 0
	holdBtn.Text = "Hold"
	holdBtn.TextColor3 = C.modeBtnTxt
	holdBtn.Font = Enum.Font.GothamBold
	holdBtn.TextSize = 12
	holdBtn.AutoButtonColor = false
	mkCorner(holdBtn, 8)
	local updateInfJumpModeUI
	updateInfJumpModeUI = function()
		local manualActive = State.infJumpMode == "manual"
		TweenService:Create(manuelBtn,TweenInfo.new(0.15),{
			BackgroundColor3 = manualActive and C.modeBtnActBg or Color3.fromRGB(0,0,0),
			BackgroundTransparency = manualActive and 0 or 1,
			TextColor3 = manualActive and C.modeBtnActTx or C.modeBtnTxt
		}):Play()
		TweenService:Create(holdBtn,TweenInfo.new(0.15),{
			BackgroundColor3 = (not manualActive) and C.modeBtnActBg or Color3.fromRGB(0,0,0),
			BackgroundTransparency = (not manualActive) and 0 or 1,
			TextColor3 = (not manualActive) and C.modeBtnActTx or C.modeBtnTxt
		}):Play()
	end
	_G.__nightmareUpdateJumpMode = updateInfJumpModeUI
	manuelBtn.MouseButton1Click:Connect(function()
		local orig = manuelBtn.BackgroundColor3
		manuelBtn.BackgroundColor3 = C.flashRed
		task.delay(0.1, function() if manuelBtn then manuelBtn.BackgroundColor3 = orig end end)
		State.infJumpMode = "manual"
		updateInfJumpModeUI()
	end)
	holdBtn.MouseButton1Click:Connect(function()
		local orig = holdBtn.BackgroundColor3
		holdBtn.BackgroundColor3 = C.flashRed
		task.delay(0.1, function() if holdBtn then holdBtn.BackgroundColor3 = orig end end)
		State.infJumpMode = "hold"
		updateInfJumpModeUI()
	end)
	updateInfJumpModeUI()
end)

-- SETTINGS PAGE
buildPage("Settings", function()
	makeGap(2)
	makeSectionHeader("Interface")
	makeGap(2)
	
	local uiScaleVal = State.uiScale
	local uiScaleRow = Instance.new("Frame", currentPage)
	uiScaleRow.Size = UDim2.new(1, 0, 0, 44)
	uiScaleRow.BackgroundTransparency = 1; uiScaleRow.BorderSizePixel = 0; uiScaleRow.LayoutOrder = LO()
	local uiScaleDiv = Instance.new("Frame", uiScaleRow)
	uiScaleDiv.Size = UDim2.new(1, -24, 0, 1); uiScaleDiv.Position = UDim2.new(0, 12, 1, -1)
	uiScaleDiv.BackgroundColor3 = C.rowBorder; uiScaleDiv.BorderSizePixel = 0
	local uiScaleLbl = Instance.new("TextLabel", uiScaleRow)
	uiScaleLbl.Size = UDim2.new(0.5, 0, 1, 0); uiScaleLbl.Position = UDim2.new(0, 12, 0, 0)
	uiScaleLbl.BackgroundTransparency = 1; uiScaleLbl.Text = "UI Scale"
	uiScaleLbl.TextColor3 = C.rowLabel; uiScaleLbl.Font = Enum.Font.GothamBold; uiScaleLbl.TextSize = 13
	uiScaleLbl.TextXAlignment = Enum.TextXAlignment.Left
	local uiScaleWrap = Instance.new("Frame", uiScaleRow)
	uiScaleWrap.Size = UDim2.new(0, 110, 0, 30); uiScaleWrap.Position = UDim2.new(1, -122, 0.5, -15)
	uiScaleWrap.BackgroundTransparency = 1; uiScaleWrap.BorderSizePixel = 0
	local uiScaleMinusBtn = Instance.new("TextButton", uiScaleWrap)
	uiScaleMinusBtn.Size = UDim2.new(0, 30, 1, 0); uiScaleMinusBtn.Position = UDim2.new(0, 0, 0, 0)
	uiScaleMinusBtn.BackgroundColor3 = C.modeBtnBg; uiScaleMinusBtn.BorderSizePixel = 0
	uiScaleMinusBtn.Text = "−"; uiScaleMinusBtn.TextColor3 = C.rowLabel
	uiScaleMinusBtn.Font = Enum.Font.GothamBold; uiScaleMinusBtn.TextSize = 16; uiScaleMinusBtn.ZIndex = 7
	mkCorner(uiScaleMinusBtn, 8); mkStroke(uiScaleMinusBtn, C.modeBtnBrd, 1)
	local uiScaleValLbl = Instance.new("TextLabel", uiScaleWrap)
	uiScaleValLbl.Size = UDim2.new(0, 44, 1, 0); uiScaleValLbl.Position = UDim2.new(0, 33, 0, 0)
	uiScaleValLbl.BackgroundTransparency = 1; uiScaleValLbl.Text = tostring(math.floor(uiScaleVal * 100)) .. "%"
	uiScaleValLbl.TextColor3 = C.rowLabel; uiScaleValLbl.Font = Enum.Font.GothamBold; uiScaleValLbl.TextSize = 12
	uiScaleValLbl.ZIndex = 7
	local uiScalePlusBtn = Instance.new("TextButton", uiScaleWrap)
	uiScalePlusBtn.Size = UDim2.new(0, 30, 1, 0); uiScalePlusBtn.Position = UDim2.new(0, 80, 0, 0)
	uiScalePlusBtn.BackgroundColor3 = C.modeBtnBg; uiScalePlusBtn.BorderSizePixel = 0
	uiScalePlusBtn.Text = "+"; uiScalePlusBtn.TextColor3 = C.rowLabel
	uiScalePlusBtn.Font = Enum.Font.GothamBold; uiScalePlusBtn.TextSize = 16; uiScalePlusBtn.ZIndex = 7
	mkCorner(uiScalePlusBtn, 8); mkStroke(uiScalePlusBtn, C.modeBtnBrd, 1)
	local function applyUIScale(val)
		uiScaleVal = math.clamp(math.floor(val * 20 + 0.5) / 20, 0.5, 1.5)
		globalScaleObj.Scale = uiScaleVal
		State.uiScale = uiScaleVal
		uiScaleValLbl.Text = tostring(math.floor(uiScaleVal * 100)) .. "%"
		task.spawn(function() if saveConfig then pcall(saveConfig) end end)
	end
	uiScaleMinusBtn.MouseButton1Click:Connect(function()
		uiScaleMinusBtn.BackgroundColor3 = C.flashRed
		task.delay(0.1, function() if uiScaleMinusBtn then uiScaleMinusBtn.BackgroundColor3 = C.modeBtnBg end end)
		applyUIScale(uiScaleVal - 0.05)
	end)
	uiScalePlusBtn.MouseButton1Click:Connect(function()
		uiScalePlusBtn.BackgroundColor3 = C.flashRed
		task.delay(0.1, function() if uiScalePlusBtn then uiScalePlusBtn.BackgroundColor3 = C.modeBtnBg end end)
		applyUIScale(uiScaleVal + 0.05)
	end)
	
	makeGap(2)
	local windowScaleVal = State.windowScale
	local windowScaleRow = Instance.new("Frame", currentPage)
	windowScaleRow.Size = UDim2.new(1, 0, 0, 44)
	windowScaleRow.BackgroundTransparency = 1; windowScaleRow.BorderSizePixel = 0; windowScaleRow.LayoutOrder = LO()
	local windowScaleDiv = Instance.new("Frame", windowScaleRow)
	windowScaleDiv.Size = UDim2.new(1, -24, 0, 1); windowScaleDiv.Position = UDim2.new(0, 12, 1, -1)
	windowScaleDiv.BackgroundColor3 = C.rowBorder; windowScaleDiv.BorderSizePixel = 0
	local windowScaleLbl = Instance.new("TextLabel", windowScaleRow)
	windowScaleLbl.Size = UDim2.new(0.5, 0, 1, 0); windowScaleLbl.Position = UDim2.new(0, 12, 0, 0)
	windowScaleLbl.BackgroundTransparency = 1; windowScaleLbl.Text = "Window Scale"
	windowScaleLbl.TextColor3 = C.rowLabel; windowScaleLbl.Font = Enum.Font.GothamBold; windowScaleLbl.TextSize = 13
	windowScaleLbl.TextXAlignment = Enum.TextXAlignment.Left
	local windowScaleWrap = Instance.new("Frame", windowScaleRow)
	windowScaleWrap.Size = UDim2.new(0, 110, 0, 30); windowScaleWrap.Position = UDim2.new(1, -122, 0.5, -15)
	windowScaleWrap.BackgroundTransparency = 1; windowScaleWrap.BorderSizePixel = 0
	local windowScaleMinusBtn = Instance.new("TextButton", windowScaleWrap)
	windowScaleMinusBtn.Size = UDim2.new(0, 30, 1, 0); windowScaleMinusBtn.Position = UDim2.new(0, 0, 0, 0)
	windowScaleMinusBtn.BackgroundColor3 = C.modeBtnBg; windowScaleMinusBtn.BorderSizePixel = 0
	windowScaleMinusBtn.Text = "−"; windowScaleMinusBtn.TextColor3 = C.rowLabel
	windowScaleMinusBtn.Font = Enum.Font.GothamBold; windowScaleMinusBtn.TextSize = 16; windowScaleMinusBtn.ZIndex = 7
	mkCorner(windowScaleMinusBtn, 8); mkStroke(windowScaleMinusBtn, C.modeBtnBrd, 1)
	local windowScaleValLbl = Instance.new("TextLabel", windowScaleWrap)
	windowScaleValLbl.Size = UDim2.new(0, 44, 1, 0); windowScaleValLbl.Position = UDim2.new(0, 33, 0, 0)
	windowScaleValLbl.BackgroundTransparency = 1; windowScaleValLbl.Text = tostring(math.floor(windowScaleVal * 100)) .. "%"
	windowScaleValLbl.TextColor3 = C.rowLabel; windowScaleValLbl.Font = Enum.Font.GothamBold; windowScaleValLbl.TextSize = 12
	windowScaleValLbl.ZIndex = 7
	local windowScalePlusBtn = Instance.new("TextButton", windowScaleWrap)
	windowScalePlusBtn.Size = UDim2.new(0, 30, 1, 0); windowScalePlusBtn.Position = UDim2.new(0, 80, 0, 0)
	windowScalePlusBtn.BackgroundColor3 = C.modeBtnBg; windowScalePlusBtn.BorderSizePixel = 0
	windowScalePlusBtn.Text = "+"; windowScalePlusBtn.TextColor3 = C.rowLabel
	windowScalePlusBtn.Font = Enum.Font.GothamBold; windowScalePlusBtn.TextSize = 16; windowScalePlusBtn.ZIndex = 7
	mkCorner(windowScalePlusBtn, 8); mkStroke(windowScalePlusBtn, C.modeBtnBrd, 1)
	local function applyWindowScale(val)
		windowScaleVal = math.clamp(math.floor(val * 20 + 0.5) / 20, 0.5, 1.5)
		windowScaleObj.Scale = windowScaleVal
		State.windowScale = windowScaleVal
		windowScaleValLbl.Text = tostring(math.floor(windowScaleVal * 100)) .. "%"
		recenterWindow()
		task.spawn(function() if saveConfig then pcall(saveConfig) end end)
	end
	windowScaleMinusBtn.MouseButton1Click:Connect(function()
		windowScaleMinusBtn.BackgroundColor3 = C.flashRed
		task.delay(0.1, function() if windowScaleMinusBtn then windowScaleMinusBtn.BackgroundColor3 = C.modeBtnBg end end)
		applyWindowScale(windowScaleVal - 0.05)
	end)
	windowScalePlusBtn.MouseButton1Click:Connect(function()
		windowScalePlusBtn.BackgroundColor3 = C.flashRed
		task.delay(0.1, function() if windowScalePlusBtn then windowScalePlusBtn.BackgroundColor3 = C.modeBtnBg end end)
		applyWindowScale(windowScaleVal + 0.05)
	end)

	makeGap(2)
	setHideButtonsToggle = makeToggleRow("Hide Buttons", State.stackButtonsHidden, function(on)
		applyStackButtonsVisible(not on)
		task.spawn(function() if saveConfig then pcall(saveConfig) end end)
	end)
	setShowIntroToggle = makeToggleRow("Show Intro", State.showIntro, function(on)
		State.showIntro = on
		task.spawn(function() if saveConfig then pcall(saveConfig) end end)
	end)
	local lockUIRow = makeToggleRow("Lock UI Position", State.uiLocked, function(on)
		State.uiLocked = on
		if lockBtn then lockBtn.Text = on and "🔒" or "🔓" end
	end)
	lockUISync = function(on)
		if lockUIRow then lockUIRow(on) end
		if unlockBtn then unlockBtn.Text = on and "LOCK" or "UNLOCK" end
	end
	
	makeGap(8)
	makeSectionHeader("Button Positions")
	makeGap(2)
	local btnRow1 = Instance.new("Frame", currentPage)
	btnRow1.Size = UDim2.new(1, 0, 0, 44)
	btnRow1.BackgroundTransparency = 1; btnRow1.BorderSizePixel = 0; btnRow1.LayoutOrder = LO()
	local resetBtn = Instance.new("TextButton", btnRow1)
	resetBtn.Size = UDim2.new(0.5, -18, 0, 34)
	resetBtn.Position = UDim2.new(0, 12, 0.5, -17)
	resetBtn.BackgroundColor3 = C.btnBg; resetBtn.BorderSizePixel = 0
	resetBtn.Text = "↺ Reset"; resetBtn.TextColor3 = C.btnTxt
	resetBtn.Font = Enum.Font.GothamBold; resetBtn.TextSize = 12; resetBtn.ZIndex = 5
	mkCorner(resetBtn, 8); mkStroke(resetBtn, C.btnBorder, 1)
	resetBtn.MouseEnter:Connect(function() TweenService:Create(resetBtn,TweenInfo.new(0.1),{BackgroundColor3=C.btnHov}):Play() end)
	resetBtn.MouseLeave:Connect(function() TweenService:Create(resetBtn,TweenInfo.new(0.1),{BackgroundColor3=C.btnBg}):Play() end)
	resetBtn.MouseButton1Click:Connect(function()
		local orig = resetBtn.BackgroundColor3
		resetBtn.BackgroundColor3 = C.flashRed
		task.delay(0.1, function() if resetBtn then resetBtn.BackgroundColor3 = orig end end)
		for i, def in ipairs(stackDefs) do
			local wrapper = stackWrappers[def.key]
			if wrapper then
				local defaultPos = getDefaultStackPos(i)
				TweenService:Create(wrapper,TweenInfo.new(0.35,Enum.EasingStyle.Back,Enum.EasingDirection.Out),{Position=defaultPos}):Play()
				State.savedButtonPositions[def.key] = {X=defaultPos.X.Offset, Y=defaultPos.Y.Offset}
			end
		end
		pcall(saveButtonPositions)
		resetBtn.Text = "✓ Reset!"
		task.delay(1.2, function() if resetBtn and resetBtn.Parent then resetBtn.Text = "↺ Reset" end end)
	end)
	local savePosBtn = Instance.new("TextButton", btnRow1)
	savePosBtn.Size = UDim2.new(0.5, -18, 0, 34)
	savePosBtn.Position = UDim2.new(0.5, 6, 0.5, -17)
	savePosBtn.BackgroundColor3 = C.modeBtnActBg; savePosBtn.BorderSizePixel = 0
	savePosBtn.Text = "💾 Save"; savePosBtn.TextColor3 = C.modeBtnActTx
	savePosBtn.Font = Enum.Font.GothamBold; savePosBtn.TextSize = 12; savePosBtn.ZIndex = 5
	mkCorner(savePosBtn, 8); mkStroke(savePosBtn, C.modeBtnBrd, 1)
	savePosBtn.MouseEnter:Connect(function() TweenService:Create(savePosBtn,TweenInfo.new(0.1),{BackgroundColor3=Color3.fromRGB(100,100,100)}):Play() end)
	savePosBtn.MouseLeave:Connect(function() TweenService:Create(savePosBtn,TweenInfo.new(0.1),{BackgroundColor3=C.modeBtnActBg}):Play() end)
	savePosBtn.MouseButton1Click:Connect(function()
		local orig = savePosBtn.BackgroundColor3
		savePosBtn.BackgroundColor3 = C.flashRed
		task.delay(0.1, function() if savePosBtn then savePosBtn.BackgroundColor3 = orig end end)
		for _, def in ipairs(stackDefs) do
			local wrapper = stackWrappers[def.key]
			if wrapper then
				State.savedButtonPositions[def.key] = {X=wrapper.Position.X.Offset, Y=wrapper.Position.Y.Offset}
			end
		end
		pcall(saveButtonPositions)
		savePosBtn.Text = "✓ Saved!"
		task.delay(1.2, function() if savePosBtn and savePosBtn.Parent then savePosBtn.Text = "💾 Save" end end)
	end)
	makeGap(8)
	makeSectionHeader("Config")
	makeGap(2)
	local configRow = Instance.new("Frame", currentPage)
	configRow.Size = UDim2.new(1, 0, 0, 44)
	configRow.BackgroundTransparency = 1; configRow.BorderSizePixel = 0; configRow.LayoutOrder = LO()
	local saveConfigBtn = Instance.new("TextButton", configRow)
	saveConfigBtn.Size = UDim2.new(1, -24, 0, 34)
	saveConfigBtn.Position = UDim2.new(0, 12, 0.5, -17)
	saveConfigBtn.BackgroundColor3 = C.modeBtnActBg; saveConfigBtn.BorderSizePixel = 0
	saveConfigBtn.Text = "💾 Save All"; saveConfigBtn.TextColor3 = C.modeBtnActTx
	saveConfigBtn.Font = Enum.Font.GothamBold; saveConfigBtn.TextSize = 12; saveConfigBtn.ZIndex = 5
	mkCorner(saveConfigBtn, 8); mkStroke(saveConfigBtn, C.modeBtnBrd, 1)
	saveConfigBtn.MouseEnter:Connect(function() TweenService:Create(saveConfigBtn,TweenInfo.new(0.1),{BackgroundColor3=Color3.fromRGB(100,100,100)}):Play() end)
	saveConfigBtn.MouseLeave:Connect(function() TweenService:Create(saveConfigBtn,TweenInfo.new(0.1),{BackgroundColor3=C.modeBtnActBg}):Play() end)
	saveConfigBtn.MouseButton1Click:Connect(function()
		local orig = saveConfigBtn.BackgroundColor3
		saveConfigBtn.BackgroundColor3 = C.flashRed
		task.delay(0.1, function() if saveConfigBtn then saveConfigBtn.BackgroundColor3 = orig end end)
		pcall(saveConfig)
		saveConfigBtn.Text = "✓ Saved!"
		task.delay(1.2, function() if saveConfigBtn and saveConfigBtn.Parent then saveConfigBtn.Text = "💾 Save All" end end)
	end)

	makeGap(10)
	local fw = Instance.new("Frame", currentPage); fw.Size = UDim2.new(1, 0, 0, 22)
	fw.BackgroundTransparency = 1; fw.BorderSizePixel = 0; fw.LayoutOrder = LO()
	local fl = Instance.new("TextLabel", fw); fl.Size = UDim2.new(1, 0, 1, 0)
	fl.BackgroundTransparency = 1; fl.Text = "DARK ANGEL HUB · v5.2"
	fl.TextColor3 = Color3.fromRGB(255, 255, 255); fl.Font = Enum.Font.Gotham; fl.TextSize = 10
	fl.TextXAlignment = Enum.TextXAlignment.Center
end)

-- Init tab states
for _, n in ipairs(TABS) do
	local t = tabBtns[n]; local active = (n == "Speed")
	t.btn.TextColor3 = active and Color3.fromRGB(255,80,80) or Color3.fromRGB(100,100,100)
	t.btn.BackgroundColor3 = active and Color3.fromRGB(20,20,20) or Color3.fromRGB(8,8,8)
	t.accent.Visible = active
	if tabPages[n] then tabPages[n].Visible = active end
end

local menuVisible = false

local function showMenu()
	mainOuter.Visible = true
	recenterWindow()
	menuVisible = true
	State.guiVisible = true
end

local function hideMenu()
	mainOuter.Visible = false
	menuVisible = false
	State.guiVisible = false
end

mainOuter.Visible = false
recenterWindow()

-- ============================================================
-- MENU BUTTON + LOCK BUTTON
-- ============================================================

local menuBtn = Instance.new("TextButton", gui)
menuBtn.Size = UDim2.new(0, 90, 0, 32)
menuBtn.Position = UDim2.new(0, 8, 0.5, 4)
menuBtn.BackgroundColor3 = Color3.fromRGB(10, 10, 20)
menuBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
menuBtn.Text = "MENU"
menuBtn.Font = Enum.Font.GothamBlack
menuBtn.TextSize = 13
menuBtn.BorderSizePixel = 0
menuBtn.AutoButtonColor = false
menuBtn.ZIndex = 29

local menuBtnCorner = Instance.new("UICorner", menuBtn)
menuBtnCorner.CornerRadius = UDim.new(0, 8)

local menuStroke = attachAnimStroke(menuBtn)

local unlockBtn = Instance.new("TextButton", gui)
unlockBtn.Size = UDim2.new(0, 90, 0, 32)
unlockBtn.Position = UDim2.new(0, 8, 0.5, -34)
unlockBtn.BackgroundColor3 = Color3.fromRGB(15, 15, 25)
unlockBtn.TextColor3 = Color3.fromRGB(255, 255, 255)
unlockBtn.Text = State.uiLocked and "LOCK" or "UNLOCK"
unlockBtn.Font = Enum.Font.GothamBlack
unlockBtn.TextSize = 13
unlockBtn.BorderSizePixel = 0
unlockBtn.AutoButtonColor = false
unlockBtn.ZIndex = 29

local lockBtnCorner = Instance.new("UICorner", unlockBtn)
lockBtnCorner.CornerRadius = UDim.new(0, 8)

local lockStroke = attachAnimStroke(unlockBtn)

menuBtn.MouseButton1Click:Connect(function()
	menuBtn.BackgroundColor3 = C.flashRed
	task.delay(0.12, function() if menuBtn then menuBtn.BackgroundColor3 = Color3.fromRGB(10,10,20) end end)
	if menuVisible then hideMenu() else showMenu() end
end)

unlockBtn.MouseButton1Click:Connect(function()
	unlockBtn.BackgroundColor3 = C.flashRed
	task.delay(0.1, function() if unlockBtn then unlockBtn.BackgroundColor3 = Color3.fromRGB(15,15,25) end end)
	State.uiLocked = not State.uiLocked
	unlockBtn.Text = State.uiLocked and "LOCK" or "UNLOCK"
	if lockBtn then lockBtn.Text = State.uiLocked and "🔒" or "🔓" end
	pcall(function() if lockUISync then lockUISync(State.uiLocked) end end)
end)

-- ============================================================
-- FPS / PING BANNER (TOP CENTER)
-- ============================================================
local banner = Instance.new("Frame", gui)
banner.Name = "DARK ANGELHUBStatsBanner"
banner.Size = UDim2.new(0, 300, 0, 42)
banner.Position = UDim2.new(0.5, -150, 0, 8)
banner.BackgroundColor3 = Color3.fromRGB(10, 10, 10)
banner.BackgroundTransparency = 0
banner.BorderSizePixel = 0
banner.ZIndex = 25
mkCorner(banner, 20)

local bannerStroke = attachAnimStroke(banner)

local fpsLbl = Instance.new("TextLabel", banner)
fpsLbl.Size = UDim2.new(0, 80, 1, 0)
fpsLbl.Position = UDim2.new(0, 10, 0, 0)
fpsLbl.BackgroundTransparency = 1
fpsLbl.Text = "FPS:--"
fpsLbl.TextColor3 = Color3.fromRGB(255, 255, 255)
fpsLbl.Font = Enum.Font.GothamBlack
fpsLbl.TextSize = 15
fpsLbl.TextXAlignment = Enum.TextXAlignment.Left
fpsLbl.TextYAlignment = Enum.TextYAlignment.Center
fpsLbl.ZIndex = 26

local centerLbl = Instance.new("TextLabel", banner)
centerLbl.Size = UDim2.new(0, 140, 1, 0)
centerLbl.Position = UDim2.new(0.5, -70, 0, 0)
centerLbl.BackgroundTransparency = 1
centerLbl.Text = "DARK ANGEL HUB\n⚡"
centerLbl.TextColor3 = Color3.fromRGB(255, 255, 255)
centerLbl.Font = Enum.Font.GothamBold
centerLbl.TextSize = 11
centerLbl.TextXAlignment = Enum.TextXAlignment.Center
centerLbl.TextYAlignment = Enum.TextYAlignment.Center
centerLbl.ZIndex = 26

local pingLbl = Instance.new("TextLabel", banner)
pingLbl.Size = UDim2.new(0, 80, 1, 0)
pingLbl.Position = UDim2.new(1, -90, 0, 0)
pingLbl.BackgroundTransparency = 1
pingLbl.Text = "--MS"
pingLbl.TextColor3 = Color3.fromRGB(255, 255, 255)
pingLbl.Font = Enum.Font.GothamBlack
pingLbl.TextSize = 15
pingLbl.TextXAlignment = Enum.TextXAlignment.Right
pingLbl.TextYAlignment = Enum.TextYAlignment.Center
pingLbl.ZIndex = 26

makeDraggable(banner)

local _frameCount = 0
RunService.RenderStepped:Connect(function()
	_frameCount = _frameCount + 1
end)
task.spawn(function()
	while true do
		task.wait(1)
		pcall(function()
			local fps = _frameCount
			_frameCount = 0
			local ping = 0
			pcall(function()
				ping = math.floor(LP:GetNetworkPing() * 1000)
			end)
			fpsLbl.Text = "FPS:" .. tostring(fps)
			pingLbl.Text = tostring(ping) .. "MS"
		end)
	end
end)

-- ============================================================
-- INFO BAR (steal progress bar)
-- ============================================================
local infoBar = Instance.new("Frame", gui)
infoBar.Name = "DARK ANGELHUBInfoBar"
infoBar.Size = UDim2.new(0, 300, 0, 52)
infoBar.Position = UDim2.new(0.5, -150, 1, -70)
infoBar.BackgroundColor3 = Color3.fromRGB(10, 10, 10)
infoBar.BackgroundTransparency = 0
infoBar.BorderSizePixel = 0
infoBar.Active = true
infoBar.ZIndex = 20
do local _c = Instance.new("UICorner", infoBar); _c.CornerRadius = UDim.new(0, 18) end
attachAnimStroke(infoBar)
makeDraggable(infoBar)

-- LEFT: percent label
stealPctLbl = Instance.new("TextLabel", infoBar)
stealPctLbl.Size = UDim2.new(0.5, 0, 0, 28)
stealPctLbl.Position = UDim2.new(0, 14, 0, 6)
stealPctLbl.BackgroundTransparency = 1
stealPctLbl.Text = "READY"
stealPctLbl.TextColor3 = Color3.fromRGB(255, 255, 255)
stealPctLbl.Font = Enum.Font.GothamBlack
stealPctLbl.TextSize = 15
stealPctLbl.TextXAlignment = Enum.TextXAlignment.Left
stealPctLbl.TextYAlignment = Enum.TextYAlignment.Center
stealPctLbl.ZIndex = 23

-- RIGHT: radius label
radLbl = Instance.new("TextLabel", infoBar)
radLbl.Size = UDim2.new(0.5, -14, 0, 28)
radLbl.Position = UDim2.new(0.5, 0, 0, 6)
radLbl.BackgroundTransparency = 1
radLbl.Text = "R: " .. tostring(Steal.StealRadius)
radLbl.TextColor3 = Color3.fromRGB(255, 255, 255)
radLbl.Font = Enum.Font.GothamBold
radLbl.TextSize = 15
radLbl.TextXAlignment = Enum.TextXAlignment.Right
radLbl.TextYAlignment = Enum.TextYAlignment.Center
radLbl.ZIndex = 23

-- BOTTOM: horizontal progress bar track
local barTrack = Instance.new("Frame", infoBar)
barTrack.Size = UDim2.new(1, -28, 0, 6)
barTrack.Position = UDim2.new(0, 14, 1, -14)
barTrack.BackgroundColor3 = Color3.fromRGB(20, 20, 20)
barTrack.BorderSizePixel = 0
barTrack.ZIndex = 21
mkCorner(barTrack, 4)

progressFill = Instance.new("Frame", barTrack)
progressFill.Size = UDim2.new(0, 0, 1, 0)
progressFill.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
progressFill.BackgroundTransparency = 0
progressFill.BorderSizePixel = 0
progressFill.ZIndex = 22
mkCorner(progressFill, 4)
local progressGradient = Instance.new("UIGradient", progressFill)
progressGradient.Color = ColorSequence.new({
	ColorSequenceKeypoint.new(0,   Color3.fromRGB(0, 0, 0)),
	ColorSequenceKeypoint.new(0.5, Color3.fromRGB(255, 255, 255)),
	ColorSequenceKeypoint.new(1,   Color3.fromRGB(0, 0, 0)),
})
progressGradient.Rotation = 0

-- Update radius label live
do
	task.spawn(function()
		while task.wait(0.5) do
			pcall(function()
				if stealRadBox and not stealRadBox:IsFocused() then stealRadBox.Text = tostring(Steal.StealRadius) end
				if radLbl then radLbl.Text = "R: " .. tostring(Steal.StealRadius) end
			end)
		end
	end)
end

local BTN_W = 72
local BTN_H = 50

-- ENVY AIMBOT TARGET HIGHLIGHT
local aimbotHighlight = Instance.new("Highlight")
aimbotHighlight.Name = "EnvyAimbotESP"
aimbotHighlight.FillColor = Color3.fromRGB(255, 255, 255)
aimbotHighlight.OutlineColor = Color3.fromRGB(0, 255, 100)
aimbotHighlight.FillTransparency = 0.5
aimbotHighlight.OutlineTransparency = 0
pcall(function() aimbotHighlight.Parent = game:GetService("CoreGui") end)
if not aimbotHighlight.Parent then aimbotHighlight.Parent = LP:WaitForChild("PlayerGui") end

-- Update highlight for Envy aimbot
task.spawn(function()
	while true do
		task.wait(0.1)
		if EnvyAimbot.enabled and EnvyAimbot.target and EnvyAimbot.target.Parent then
			local char = EnvyAimbot.target.Parent
			if char and char:IsA("Model") then
				aimbotHighlight.Adornee = char
			else
				aimbotHighlight.Adornee = EnvyAimbot.target
			end
		else
			aimbotHighlight.Adornee = nil
		end
	end
end)

-- APPLY STACK BUTTONS VISIBILITY
local function applyStackButtonsVisible(visible)
	State.stackButtonsHidden = not visible
	for _, wrapper in pairs(stackWrappers) do
		if wrapper then wrapper.Visible = visible end
	end
end

-- STACK BUTTONS
for i, def in ipairs(stackDefs) do
	local btnFrame = Instance.new("Frame", gui)
	btnFrame.Name = "StackBtn_"..def.key
	btnFrame.Size = UDim2.new(0, BTN_W, 0, BTN_H)
	btnFrame.Position = getDefaultStackPos(i)
	btnFrame.BackgroundColor3 = C.stackBg; btnFrame.BorderSizePixel = 0
	btnFrame.Active = true; btnFrame.ZIndex = 15
	local _btnCorner = Instance.new("UICorner", btnFrame); _btnCorner.CornerRadius = UDim.new(0, 16)
	local bStroke = attachAnimStroke(btnFrame)
	stackWrappers[def.key] = btnFrame
	local nl = Instance.new("TextLabel", btnFrame)
	nl.Size = UDim2.new(1, -6, 1, -14); nl.Position = UDim2.new(0, 3, 0, 3)
	nl.BackgroundTransparency = 1; nl.Text = def.label; nl.TextColor3 = C.stackTxt
	nl.Font = Enum.Font.Code; nl.TextSize = 13; nl.TextWrapped = true
	nl.TextXAlignment = Enum.TextXAlignment.Center; nl.ZIndex = 6
	local btnState = false
	local function setOn(on)
		btnState = on
		TweenService:Create(nl, TweenInfo.new(0.15), {TextColor3 = on and Color3.fromRGB(0, 255, 100) or Color3.fromRGB(160, 160, 160)}):Play()
	end
	stackBtnRefs[def.key] = {setOn = setOn}
	btnFrame.MouseEnter:Connect(function() end)
	btnFrame.MouseLeave:Connect(function() end)
	local function onTap()
		if def.key == "tunt" then
			local ns = not btnState
			setOn(ns)
			if ns then
				State.tuntActive = true
				State.tuntLoop = task.spawn(function()
					while State.tuntActive do
						pcall(function()
							local msg = "DARK ANGEL MOGS U AND IT'S BETTER"
							local TCS = game:GetService("TextChatService")
							local ch = TCS.TextChannels:FindFirstChild("RBXGeneral")
							if ch then
								ch:SendAsync(msg)
							else
								local rep = game:GetService("ReplicatedStorage")
								local evts = rep:FindFirstChild("DefaultChatSystemChatEvents")
								if evts then
									local sayReq = evts:FindFirstChild("SayMessageRequest")
									if sayReq then sayReq:FireServer(msg, "All") end
								end
							end
						end)
						task.wait(0.5)
					end
				end)
			else
				State.tuntActive = false
				if State.tuntLoop then
					task.cancel(State.tuntLoop)
					State.tuntLoop = nil
				end
			end
			return
		elseif def.key == "tpDown" then
			doTpDown()
		elseif def.key == "carrySpeed" then
			State.speedToggled = not State.speedToggled
			setOn(State.speedToggled)
			task.spawn(function() if saveConfig then pcall(saveConfig) end end)
		elseif def.key == "aimbot" then
			local ns = not btnState
			setOn(ns)
			if ns then
				if State.autoLeftEnabled then State.autoLeftEnabled=false; stopAutoLeft(); if stackBtnRefs.autoLeft then stackBtnRefs.autoLeft.setOn(false) end end
				if State.autoRightEnabled then State.autoRightEnabled=false; stopAutoRight(); if stackBtnRefs.autoRight then stackBtnRefs.autoRight.setOn(false) end end
			end
			setEnvyAimbotState(ns)
			if setBatAimbot then setBatAimbot(ns) end
		else
			local ns = not btnState
			setOn(ns)
			if def.key == "autoLeft" then
				if ns then
					if State.autoRightEnabled then State.autoRightEnabled=false; stopAutoRight(); if stackBtnRefs.autoRight then stackBtnRefs.autoRight.setOn(false) end end
					if EnvyAimbot.enabled then setEnvyAimbotState(false); if stackBtnRefs.aimbot then stackBtnRefs.aimbot.setOn(false) end end
					State.autoLeftEnabled = true; startAutoLeft()
				else
					State.autoLeftEnabled = false; stopAutoLeft()
				end
			elseif def.key == "autoRight" then
				if ns then
					if State.autoLeftEnabled then State.autoLeftEnabled=false; stopAutoLeft(); if stackBtnRefs.autoLeft then stackBtnRefs.autoLeft.setOn(false) end end
					if EnvyAimbot.enabled then setEnvyAimbotState(false); if stackBtnRefs.aimbot then stackBtnRefs.aimbot.setOn(false) end end
					State.autoRightEnabled = true; startAutoRight()
				else
					State.autoRightEnabled = false; stopAutoRight()
				end
			elseif def.key == "lagger" then
				State.laggerEnabled = ns
				if ns then
					State._prevCarry = State.carrySpeed
					State._prevSpeed = State.speedToggled
					State.speedToggled = false
					if stackBtnRefs.carrySpeed then stackBtnRefs.carrySpeed.setOn(false) end
					if carryBox then carryBox.Text = tostring(State.laggerSpeed) end
				else
					State.carrySpeed = State._prevCarry or 30
					State.speedToggled = State._prevSpeed or false
					if carryBox then carryBox.Text = tostring(State.carrySpeed) end
					if stackBtnRefs.carrySpeed then stackBtnRefs.carrySpeed.setOn(State.speedToggled) end
				end
			elseif def.key == "drop" then
				if ns then runDropBrainrot() else stopDropBrainrot() end
			end
			task.spawn(function() if saveConfig then pcall(saveConfig) end end)
		end
	end
	makeStackDraggable(btnFrame, onTap, def.key)
end

-- ============================================================
-- REMAINING FUNCTIONS (anti-ragdoll, steal, etc.)
-- ============================================================
local function resetProgressBar()
	if stealPctLbl then stealPctLbl.Text = "READY" end
	if progressFill then progressFill.Size = UDim2.new(0, 0, 1, 0) end
end

doTpDown = function()
	pcall(function()
		local c=LP.Character; if not c then return end
		local root=c:FindFirstChild("HumanoidRootPart"); if not root then return end
		local rp=RaycastParams.new(); rp.FilterDescendantsInstances={c}; rp.FilterType=Enum.RaycastFilterType.Exclude
		local res=workspace:Raycast(root.Position,Vector3.new(0,-1000,0),rp)
		if res then
			root.CFrame=CFrame.new(res.Position+Vector3.new(0,root.Size.Y/2+0.5,0)); root.AssemblyLinearVelocity=Vector3.zero
		end
	end)
end

-- DROP BRAINROT
local dropBrainrotActive = false
local function dropBrainrotNow()
	if dropBrainrotActive then return end
	local char = LP.Character
	if not char then return end
	local root = char:FindFirstChild("HumanoidRootPart")
	if not root then return end
	dropBrainrotActive = true
	local t0 = tick()
	local DROP_ASCEND_DURATION = 0.2
	local DROP_ASCEND_SPEED = 150
	local dc
	dc = RunService.Heartbeat:Connect(function()
		local r = char and char:FindFirstChild("HumanoidRootPart")
		if not r then
			dc:Disconnect()
			dropBrainrotActive = false
			return
		end
		if tick() - t0 >= DROP_ASCEND_DURATION then
			dc:Disconnect()
			local rp = RaycastParams.new()
			rp.FilterDescendantsInstances = { char }
			rp.FilterType = Enum.RaycastFilterType.Exclude
			local rr = workspace:Raycast(r.Position, Vector3.new(0, -2000, 0), rp)
			if rr then
				local hum = char:FindFirstChildOfClass("Humanoid")
				local off = (hum and hum.HipHeight or 2) + (r.Size.Y / 2)
				r.CFrame = CFrame.new(r.Position.X, rr.Position.Y + off, r.Position.Z)
				r.AssemblyLinearVelocity = Vector3.new(0, 0, 0)
			end
			dropBrainrotActive = false
			return
		end
		r.AssemblyLinearVelocity = Vector3.new(r.AssemblyLinearVelocity.X, DROP_ASCEND_SPEED, r.AssemblyLinearVelocity.Z)
	end)
end
local dropBRConn = nil
local function startDropBR()
	if dropBRConn then return end
	dropBRConn = RunService.Heartbeat:Connect(function()
		if State.dropEnabled then dropBrainrotNow() end
	end)
end
local function stopDropBR()
	if dropBRConn then dropBRConn:Disconnect(); dropBRConn = nil end
	dropBrainrotActive = false
end
runDropBrainrot = function()
	if State.dropEnabled then return end
	State.dropEnabled = true
	if stackBtnRefs.drop then stackBtnRefs.drop.setOn(true) end
	startDropBR()
	task.delay(DROP_AUTO_OFF_DELAY, function()
		State.dropEnabled = false
		stopDropBR()
		if stackBtnRefs.drop then stackBtnRefs.drop.setOn(false) end
	end)
end
stopDropBrainrot = function()
	State.dropEnabled = false
	stopDropBR()
	if stackBtnRefs.drop then stackBtnRefs.drop.setOn(false) end
end

-- Bat Counter
local BAT_COUNTER_SLAP_LIST={"Bat","Slap","Iron Slap","Gold Slap","Diamond Slap","Emerald Slap","Ruby Slap","Dark Matter Slap","Flame Slap","Nuclear Slap","Galaxy Slap","Glitched Slap"}
local function findBatForCounter()
	local c=LP.Character; if not c then return nil end
	local bp=LP:FindFirstChildOfClass("Backpack")
	for _,name in ipairs(BAT_COUNTER_SLAP_LIST) do
		local t=c:FindFirstChild(name) or (bp and bp:FindFirstChild(name))
		if t then return t end
	end
	for _,ch in ipairs(c:GetChildren()) do if ch:IsA("Tool") and ch.Name:lower():find("bat") then return ch end end
	if bp then for _,ch in ipairs(bp:GetChildren()) do if ch:IsA("Tool") and ch.Name:lower():find("bat") then return ch end end end
	return nil
end
local function swingBatForCounter(bat,char)
	local hum2=char:FindFirstChildOfClass("Humanoid")
	if bat.Parent~=char then if hum2 then pcall(function() hum2:EquipTool(bat) end) end; task.wait(0.05) end
	local remote=bat:FindFirstChildOfClass("RemoteEvent") or bat:FindFirstChildOfClass("RemoteFunction")
	if remote and remote:IsA("RemoteEvent") then
		pcall(function() remote:FireServer() end); task.wait(0.15); pcall(function() remote:FireServer() end)
	else
		pcall(function() bat:Activate() end); task.wait(0.15); pcall(function() bat:Activate() end)
	end
end
startBatCounter=function()
	if Conns.batCounter then return end
	Conns.batCounter=RunService.Heartbeat:Connect(function()
		if not State.batCounterEnabled then return end
		if State.batCounterDebounce then return end
		local char=LP.Character; if not char then return end
		local hum2=char:FindFirstChildOfClass("Humanoid"); if not hum2 then return end
		local st=hum2:GetState()
		local isRagdolled=st==Enum.HumanoidStateType.Physics or st==Enum.HumanoidStateType.Ragdoll or st==Enum.HumanoidStateType.FallingDown
		if isRagdolled then
			State.batCounterDebounce=true
			task.spawn(function()
				local bat=findBatForCounter()
				if bat then swingBatForCounter(bat,char) end
				task.wait(0.5); State.batCounterDebounce=false
			end)
		end
	end)
end
stopBatCounter=function()
	if Conns.batCounter then Conns.batCounter:Disconnect(); Conns.batCounter=nil end
	State.batCounterDebounce=false
end

-- Medusa counter, anti-ragdoll, unwalk, FPS boost
local function findMedusa()
	local c=LP.Character; if not c then return nil end
	for _,t in ipairs(c:GetChildren()) do if t:IsA("Tool") then local n=t.Name:lower(); if n:find("medusa") or n:find("head") or n:find("stone") then return t end end end
	local bp=LP:FindFirstChild("Backpack")
	if bp then for _,t in ipairs(bp:GetChildren()) do if t:IsA("Tool") then local n=t.Name:lower(); if n:find("medusa") or n:find("head") or n:find("stone") then return t end end end end
	return nil
end
local function useMedusaCounter()
	if State.medusaDebounce then return end; if tick()-State.medusaLastUsed<MEDUSA_COOLDOWN then return end
	local c=LP.Character; if not c then return end; State.medusaDebounce=true
	local med=findMedusa(); if not med then State.medusaDebounce=false; return end
	if med.Parent~=c then local hum2=c:FindFirstChildOfClass("Humanoid"); if hum2 then hum2:EquipTool(med) end end
	State.batAimbotPaused = true
	pcall(function() med:Activate() end)
	State.medusaLastUsed=tick(); State.medusaDebounce=false
	task.delay(0.6, function() State.batAimbotPaused = false end)
end
local function onAnchorChanged(part)
	return part:GetPropertyChangedSignal("Anchored"):Connect(function()
		if part.Anchored and part.Transparency==1 then useMedusaCounter() end
	end)
end
setupMedusaCounter=function(char)
	stopMedusaCounter(); if not char then return end
	for _,part in ipairs(char:GetDescendants()) do
		if part:IsA("BasePart") then table.insert(Conns.anchor,onAnchorChanged(part)) end
	end
	table.insert(Conns.anchor,char.DescendantAdded:Connect(function(part)
		if part:IsA("BasePart") then table.insert(Conns.anchor,onAnchorChanged(part)) end
	end))
end
stopMedusaCounter=function()
	for _,c2 in pairs(Conns.anchor) do pcall(function() c2:Disconnect() end) end; Conns.anchor={}
end

local function faceSouth()
	pcall(function() local c=LP.Character; if not c then return end; local root=c:FindFirstChild("HumanoidRootPart") if root then root.CFrame=CFrame.new(root.Position)*CFrame.Angles(0,0,0) end end)
end
local function faceNorth()
	pcall(function() local c=LP.Character; if not c then return end; local root=c:FindFirstChild("HumanoidRootPart") if root then root.CFrame=CFrame.new(root.Position)*CFrame.Angles(0,math.rad(180),0) end end)
end
startAutoLeft=function()
	if Conns.autoLeft then Conns.autoLeft:Disconnect() end; State.autoLeftPhase=1; State.waypointIdx=1
	Conns.autoLeft=RunService.Heartbeat:Connect(function()
		if not State.autoLeftEnabled then return end
		local c=LP.Character; if not c then return end
		local root=c:FindFirstChild("HumanoidRootPart"); local hum2=c:FindFirstChildOfClass("Humanoid")
		if not root or not hum2 then return end
		local spd = (State.waypointIdx <= 2) and 57 or 28
		local wp=POS.leftWaypoints[State.waypointIdx]
		if not wp then return end
		local tgt=Vector3.new(wp.X,root.Position.Y,wp.Z)
		if (tgt-root.Position).Magnitude<2.5 then
			State.waypointIdx=State.waypointIdx+1
			if State.waypointIdx>#POS.leftWaypoints then
				hum2:Move(Vector3.zero,false); root.AssemblyLinearVelocity=Vector3.zero
				State.autoLeftEnabled=false; if Conns.autoLeft then Conns.autoLeft:Disconnect(); Conns.autoLeft=nil end
				State.autoLeftPhase=1; State.waypointIdx=1; if stackBtnRefs.autoLeft then stackBtnRefs.autoLeft.setOn(false) end
				faceSouth(); return
			end
			return
		end
		local d=(wp-root.Position); local mv=Vector3.new(d.X,0,d.Z).Unit
		hum2:Move(mv,false); root.AssemblyLinearVelocity=Vector3.new(mv.X*spd,root.AssemblyLinearVelocity.Y,mv.Z*spd)
	end)
end
stopAutoLeft=function()
	if Conns.autoLeft then Conns.autoLeft:Disconnect(); Conns.autoLeft=nil end; State.autoLeftPhase=1
	local c=LP.Character; if c then local hum2=c:FindFirstChildOfClass("Humanoid"); if hum2 then hum2:Move(Vector3.zero,false) end end
	if stackBtnRefs.autoLeft then stackBtnRefs.autoLeft.setOn(false) end
end
startAutoRight=function()
	if Conns.autoRight then Conns.autoRight:Disconnect() end; State.autoRightPhase=1; State.waypointIdx=1
	Conns.autoRight=RunService.Heartbeat:Connect(function()
		if not State.autoRightEnabled then return end
		local c=LP.Character; if not c then return end
		local root=c:FindFirstChild("HumanoidRootPart"); local hum2=c:FindFirstChildOfClass("Humanoid")
		if not root or not hum2 then return end
		local spd = (State.waypointIdx <= 2) and 57 or 28
		local wp=POS.rightWaypoints[State.waypointIdx]
		if not wp then return end
		local tgt=Vector3.new(wp.X,root.Position.Y,wp.Z)
		if (tgt-root.Position).Magnitude<2.5 then
			State.waypointIdx=State.waypointIdx+1
			if State.waypointIdx>#POS.rightWaypoints then
				hum2:Move(Vector3.zero,false); root.AssemblyLinearVelocity=Vector3.zero
				State.autoRightEnabled=false; if Conns.autoRight then Conns.autoRight:Disconnect(); Conns.autoRight=nil end
				State.autoRightPhase=1; State.waypointIdx=1; if stackBtnRefs.autoRight then stackBtnRefs.autoRight.setOn(false) end
				faceNorth(); return
			end
			return
		end
		local d=(wp-root.Position); local mv=Vector3.new(d.X,0,d.Z).Unit
		hum2:Move(mv,false); root.AssemblyLinearVelocity=Vector3.new(mv.X*spd,root.AssemblyLinearVelocity.Y,mv.Z*spd)
	end)
end
stopAutoRight=function()
	if Conns.autoRight then Conns.autoRight:Disconnect(); Conns.autoRight=nil end; State.autoRightPhase=1
	local c=LP.Character; if c then local hum2=c:FindFirstChildOfClass("Humanoid"); if hum2 then hum2:Move(Vector3.zero,false) end end
	if stackBtnRefs.autoRight then stackBtnRefs.autoRight.setOn(false) end
end

local Conns_duelWatch = nil
local function startDuelCountdownWatcher(direction)
	if Conns_duelWatch then Conns_duelWatch:Disconnect(); Conns_duelWatch=nil end
	State._duelWaiting = true
	local function countdownVisible()
		local pg = LP:FindFirstChild("PlayerGui"); if not pg then return false end
		for _, obj in ipairs(pg:GetDescendants()) do
			if obj:IsA("TextLabel") then
				local t = obj.Text
				if t == "3" or t == "2" or t == "1" or t == "GO!" or t == "Go!" then
					if obj.Visible then return true end
				end
			end
			if obj:IsA("ScreenGui") then
				local n = obj.Name:lower()
				if (n:find("duel") or n:find("countdown") or n:find("battle")) and obj.Enabled then return true end
			end
		end
		return false
	end
	local sawCountdown = false
	Conns_duelWatch = RunService.Heartbeat:Connect(function()
		if not State.duelCountdownEnabled then
			Conns_duelWatch:Disconnect(); Conns_duelWatch=nil; State._duelWaiting=false
			return
		end
		local visible = countdownVisible()
		if not sawCountdown and visible then sawCountdown = true end
		if sawCountdown and not visible then
			Conns_duelWatch:Disconnect(); Conns_duelWatch=nil; State._duelWaiting=false
			task.wait(0.05)
			if direction == "left" then
				State.autoLeftEnabled=true; if stackBtnRefs.autoLeft then stackBtnRefs.autoLeft.setOn(true) end; startAutoLeft()
			elseif direction == "right" then
				State.autoRightEnabled=true; if stackBtnRefs.autoRight then stackBtnRefs.autoRight.setOn(true) end; startAutoRight()
			end
			task.delay(2, function() if State.duelCountdownEnabled then startDuelCountdownWatcher(direction) end end)
		end
	end)
end
local function stopDuelCountdownWatcher()
	if Conns_duelWatch then Conns_duelWatch:Disconnect(); Conns_duelWatch=nil end
	State._duelWaiting = false
end

startAntiRagdoll=function()
	if Conns.antiRag then return end
	Conns.antiRag=RunService.Heartbeat:Connect(function()
		if not State.antiRagdollEnabled then return end
		local c=LP.Character; if not c then return end
		local hum2=c:FindFirstChildOfClass("Humanoid"); local root=c:FindFirstChild("HumanoidRootPart")
		if not hum2 or not root then return end; if hum2.Health<=0 then return end
		local st=hum2:GetState(); if st==Enum.HumanoidStateType.Dead then return end
		if st==Enum.HumanoidStateType.Physics or st==Enum.HumanoidStateType.Ragdoll or st==Enum.HumanoidStateType.FallingDown then
			pcall(function() hum2:ChangeState(Enum.HumanoidStateType.GettingUp) end)
			pcall(function() workspace.CurrentCamera.CameraSubject=hum2 end)
			pcall(function() local PM=LP.PlayerScripts:FindFirstChild("PlayerModule") if PM then local CM=require(PM:FindFirstChild("ControlModule")); if CM then CM:Enable() end end end)
			root.Velocity=Vector3.new(0,0,0); root.RotVelocity=Vector3.new(0,0,0)
		end
		for _,obj in ipairs(c:GetDescendants()) do
			pcall(function() if obj:IsA("Motor6D") and obj.Enabled==false then obj.Enabled=true end end)
		end
	end)
end
stopAntiRagdoll=function()
	if Conns.antiRag then Conns.antiRag:Disconnect(); Conns.antiRag=nil end
end

local unwalkAnimateRef=nil
local function startUnwalk()
	local c=LP.Character; if not c then return end
	local hum2=c:FindFirstChildOfClass("Humanoid")
	if hum2 then pcall(function() for _,track in ipairs(hum2:GetPlayingAnimationTracks()) do track:Stop(0) end end) end
	local animCtrl=c:FindFirstChildOfClass("AnimationController")
	if animCtrl then pcall(function() for _,track in ipairs(animCtrl:GetPlayingAnimationTracks()) do track:Stop(0) end end) end
	local anim=c:FindFirstChild("Animate")
	if anim and anim:IsA("LocalScript") then anim.Disabled=true; unwalkAnimateRef=anim end
	if Conns.unwalk then Conns.unwalk:Disconnect() end
	Conns.unwalk=RunService.Heartbeat:Connect(function()
		if not State.unwalkEnabled then return end
		local c2=LP.Character; if not c2 then return end
		local hum3=c2:FindFirstChildOfClass("Humanoid")
		if hum3 then pcall(function() for _,track in ipairs(hum3:GetPlayingAnimationTracks()) do track:Stop(0) end end) end
	end)
end
local function stopUnwalk()
	if Conns.unwalk then Conns.unwalk:Disconnect(); Conns.unwalk=nil end
	local c=LP.Character
	if c and unwalkAnimateRef and unwalkAnimateRef.Parent==c then unwalkAnimateRef.Disabled=false end
	unwalkAnimateRef=nil
end

applyFPSBoost=function()
	pcall(function() setfpscap(999999999) end)
	local function pO(v)
		pcall(function()
			if v:IsA("Model") then
				v.LevelOfDetail=Enum.ModelLevelOfDetail.Disabled; v.ModelStreamingMode=Enum.ModelStreamingMode.Nonatomic
			elseif v:IsA("MeshPart") then
				v.CastShadow=false; v.DoubleSided=false; v.RenderFidelity=Enum.RenderFidelity.Performance
			elseif v:IsA("BasePart") then
				v.CastShadow=false; v.Material=Enum.Material.Plastic; v.Reflectance=0
			elseif v:IsA("Decal") or v:IsA("Texture") then
				v.Transparency=1
			elseif v:IsA("SpecialMesh") then
				v.TextureId=""
			elseif v:IsA("Fire") or v:IsA("SpotLight") or v:IsA("Smoke") or v:IsA("Sparkles") or v:IsA("ParticleEmitter") or v:IsA("Trail") or v:IsA("Beam") then
				v.Enabled=false
			elseif v:IsA("SurfaceAppearance") or v:IsA("MaterialVariant") then
				v:Destroy()
			elseif v:IsA("Attachment") then
				v.Visible=false
			end
		end)
	end
	for _,v in pairs(workspace:GetDescendants()) do pO(v) end
	pcall(function()
		local L=game:GetService("Lighting")
		for _,v in pairs(L:GetDescendants()) do
			pcall(function() if v:IsA("Sky") or v:IsA("Atmosphere") or v:IsA("BloomEffect") or v:IsA("BlurEffect") or v:IsA("SunRaysEffect") or v:IsA("DepthOfFieldEffect") or v:IsA("Clouds") or v:IsA("PostEffect") or v:IsA("ColorCorrectionEffect") then v:Destroy() end end)
		end
		pcall(function() sethiddenproperty(L,"Technology",Enum.Technology.Legacy) end)
		L.GlobalShadows=false; L.FogEnd=9e9; L.Brightness=0
		local ter=workspace:FindFirstChildOfClass("Terrain")
		if ter then pcall(function() sethiddenproperty(ter,"Decoration",false) end); ter.WaterReflectance=0; ter.WaterTransparency=0.7; ter.WaterWaveSize=0; ter.WaterWaveSpeed=0 end
	end)
	workspace.DescendantAdded:Connect(function(v) if State.fpsBoostEnabled then task.spawn(pO,v) end end)
end

-- ============================================================
-- SAVE CONFIG
-- ============================================================
local CONFIG_FILE = "Dark AngelHubConfig.json"
saveConfig = function()
	local cfg = {
		normalSpeed = State.normalSpeed,
		carrySpeed = State.carrySpeed,
		laggerSpeed = State.laggerSpeed,
		stealRadius = Steal.StealRadius,
		stealDuration = Steal.StealDuration,
		autoStealEnabled = Steal.AutoStealEnabled,
		sizeMode = "Medium",
		stackButtonsHidden = State.stackButtonsHidden,
		prcMode = State.prcMode,
		uiLocked = State.uiLocked,
		fov = currentFOV,
		skyMode = activeSkyMode,
		speedKey = Keys.speed.Name,
		autoLeftKey = Keys.autoLeft.Name,
		autoRightKey = Keys.autoRight.Name,
		guiHideKey = Keys.guiHide.Name,
		dropKey = Keys.drop.Name,
		laggerKey = Keys.lagger.Name,
		tpDownKey = Keys.tpDown.Name,
		aimbotKey = Keys.aimbot.Name,
		infJump = State.infJumpEnabled,
		jumpPower = State.jumpPower,
		infJumpMode = State.infJumpMode,
		antiRagdoll = State.antiRagdollEnabled,
		fpsBoost = State.fpsBoostEnabled,
		medusaCounter = State.medusaCounterEnabled,
		unwalkEnabled = State.unwalkEnabled,
		lockInEnabled = lockInEnabled,
		batCounter = State.batCounterEnabled,
		envyAimbotEnabled = EnvyAimbot.enabled,
		envySpeed = EnvyAimbot.speed,
		envyVertSpeed = EnvyAimbot.vertSpeed,
		envyDistance = EnvyAimbot.distance,
		envyHeight = EnvyAimbot.height,
		envyVertOffset = EnvyAimbot.vertOffset,
		envyTurnSpeed = EnvyAimbot.turnSpeed,
		envyMaxTurnRate = EnvyAimbot.maxTurnRate,
		envySwingEnabled = EnvyAimbot.swingEnabled,
		speedToggled = State.speedToggled,
		laggerEnabled = State.laggerEnabled,
		duelCountdownEnabled = State.duelCountdownEnabled,
		duelDir = duelDir,
		autoLeftEnabled = State.autoLeftEnabled,
		autoRightEnabled = State.autoRightEnabled,
		stretchRez = State.stretchRez,
		nightTime = State.nightTime,
		purpleSky = State.purpleSky,
		fpsBoosterCursed = State.fpsBoosterCursed,
		playerEsp = State.playerEsp,
		antiLagClean = State.antiLagClean,
		cloudyTime = State.cloudyTime,
		optimizeEnabled = State.optimizerEnabled,
		xrayEnabled = State.xrayEnabled,
		uiScale = State.uiScale,
		windowScale = State.windowScale,
		animPackActive = _G._nightmareAnimPackActive or nil,
		showIntro = State.showIntro,
		bypassActivated = bypassActivated,
		bypassPower = bypassPower,
	}
	local ok, encoded = pcall(function() return HttpService:JSONEncode(cfg) end)
	if ok then pcall(function() _writefile(CONFIG_FILE, encoded) end) end
end

loadConfig = function()
	local hasFile = false; pcall(function() hasFile = _isfile(CONFIG_FILE) end)
	if not hasFile then return end
	local raw; local ok = pcall(function() raw = _readfile(CONFIG_FILE) end)
	if not ok or not raw then return end
	local cfg; local ok2 = pcall(function() cfg = HttpService:JSONDecode(raw) end)
	if not ok2 or not cfg then return end

	if cfg.normalSpeed then State.normalSpeed = cfg.normalSpeed; if normalBox then normalBox.Text = tostring(cfg.normalSpeed) end end
	if cfg.carrySpeed then State.carrySpeed = cfg.carrySpeed; if carryBox then carryBox.Text = tostring(cfg.carrySpeed) end end
	if cfg.laggerSpeed then State.laggerSpeed = cfg.laggerSpeed; if laggerBox then laggerBox.Text = tostring(cfg.laggerSpeed) end end
	if cfg.stealRadius then Steal.StealRadius = cfg.stealRadius; if stealRadBox then stealRadBox.Text = tostring(cfg.stealRadius) end end
	if cfg.stealDuration then Steal.StealDuration = cfg.stealDuration end
	if cfg.sizeMode and _G.applySizeActive then pcall(function() _G.applySizeActive(cfg.sizeMode) end) end
	if cfg.stackButtonsHidden then applyStackButtonsVisible(false); if setHideButtonsToggle then setHideButtonsToggle(true) end end
	if cfg.prcMode then pcall(function() applyPRCMode(cfg.prcMode) end) end
	if cfg.uiLocked ~= nil then
		State.uiLocked = cfg.uiLocked
		if lockBtn then lockBtn.Text = cfg.uiLocked and "🔒" or "🔓" end
		pcall(function() if lockUISync then lockUISync(cfg.uiLocked) end end)
	end
	if cfg.fov and cfg.fov >= 60 and cfg.fov <= 200 then
		currentFOV = cfg.fov
		pcall(function() workspace.CurrentCamera.FieldOfView = currentFOV end)
		if setFOVExternal then pcall(function() setFOVExternal(currentFOV) end) end
	end
	if cfg.skyMode then
		activeSkyMode = cfg.skyMode
		task.spawn(function()
			local waited = 0
			while not applySkyMode and waited < 10 do
				task.wait(0.1); waited = waited + 0.1
			end
			if applySkyMode then pcall(function() applySkyMode(cfg.skyMode) end) end
		end)
	end
	if cfg.lockInEnabled then
		lockInEnabled = true
		if setLockInToggle then pcall(function() setLockInToggle(true) end) end
		pcall(function()
			local c = LP.Character
			if c then saveOriginalAnims(c); applyAnimPack(c) end
			if lockInHeartbeatConn then lockInHeartbeatConn:Disconnect() end
			lockInHeartbeatConn = RunService.Heartbeat:Connect(function()
				if not lockInEnabled then
					if lockInHeartbeatConn then lockInHeartbeatConn:Disconnect() end
					return
				end
				local c2 = LP.Character; if c2 then applyAnimPack(c2) end
			end)
		end)
	end
	local function tryKey(field, keyTarget)
		if cfg[field] and Enum.KeyCode[cfg[field]] then
			local kc = Enum.KeyCode[cfg[field]]
			Keys[keyTarget] = kc
			if keybindBtnRefs[keyTarget] then keybindBtnRefs[keyTarget].Text = getKeyDisplayName(kc) end
		end
	end
	tryKey("speedKey", "speed"); tryKey("autoLeftKey", "autoLeft"); tryKey("autoRightKey","autoRight")
	tryKey("guiHideKey", "guiHide"); tryKey("dropKey", "drop"); tryKey("laggerKey", "lagger")
	tryKey("tpDownKey", "tpDown"); tryKey("aimbotKey", "aimbot")
	if cfg.autoStealEnabled then
		Steal.AutoStealEnabled = true
		if setInstaGrab then setInstaGrab(true) end
		startAutoSteal()
	else
		Steal.AutoStealEnabled = false
		if setInstaGrab then setInstaGrab(false) end
		stopAutoSteal()
	end
	if cfg.infJump then State.infJumpEnabled = true; if setInfJump then setInfJump(true) end end
	if cfg.jumpPower and cfg.jumpPower >= 1 and cfg.jumpPower <= 200 then State.jumpPower = cfg.jumpPower end
	if cfg.infJumpMode then
		State.infJumpMode = cfg.infJumpMode
		pcall(function() if _G.__nightmareUpdateJumpMode then _G.__nightmareUpdateJumpMode() end end)
	end
	if cfg.antiRagdoll then State.antiRagdollEnabled = true; if setAntiRag then setAntiRag(true) end; startAntiRagdoll() end
	if cfg.fpsBoost then State.fpsBoostEnabled = true; if setFps then setFps(true) end; applyFPSBoost() end
	if cfg.medusaCounter then State.medusaCounterEnabled = true; if setMedusaCounter then setMedusaCounter(true) end; setupMedusaCounter(LP.Character) end
	if cfg.batCounter then State.batCounterEnabled = true; if setBatCounter then setBatCounter(true) end; startBatCounter() end
	if cfg.unwalkEnabled then State.unwalkEnabled = true; if setUnwalkToggle then setUnwalkToggle(true) end; startUnwalk() end
	if cfg.speedToggled then State.speedToggled = true; if stackBtnRefs.carrySpeed then stackBtnRefs.carrySpeed.setOn(true) end end
	if cfg.laggerEnabled then
		State.laggerEnabled = true
		State._prevCarry = State.carrySpeed
		State._prevSpeed = State.speedToggled
		State.speedToggled = false
		if stackBtnRefs.carrySpeed then stackBtnRefs.carrySpeed.setOn(false) end
		if stackBtnRefs.lagger then stackBtnRefs.lagger.setOn(true) end
	end
	if cfg.autoLeftEnabled then
		State.autoLeftEnabled = true
		if stackBtnRefs.autoLeft then stackBtnRefs.autoLeft.setOn(true) end
		task.delay(0.5, function() pcall(startAutoLeft) end)
	end
	if cfg.autoRightEnabled then
		State.autoRightEnabled = true
		if stackBtnRefs.autoRight then stackBtnRefs.autoRight.setOn(true) end
		task.delay(0.5, function() pcall(startAutoRight) end)
	end
	
	-- Envy Aimbot settings
	if cfg.envyAimbotEnabled then
		setEnvyAimbotState(true)
		if stackBtnRefs.aimbot then stackBtnRefs.aimbot.setOn(true) end
		if setBatAimbot then setBatAimbot(true) end
	end
	if cfg.envySpeed then EnvyAimbot.speed = cfg.envySpeed end
	if cfg.envyVertSpeed then EnvyAimbot.vertSpeed = cfg.envyVertSpeed end
	if cfg.envyDistance then EnvyAimbot.distance = cfg.envyDistance end
	if cfg.envyHeight then EnvyAimbot.height = cfg.envyHeight end
	if cfg.envyVertOffset then EnvyAimbot.vertOffset = cfg.envyVertOffset end
	if cfg.envyTurnSpeed then EnvyAimbot.turnSpeed = cfg.envyTurnSpeed end
	if cfg.envyMaxTurnRate then EnvyAimbot.maxTurnRate = cfg.envyMaxTurnRate end
	if cfg.envySwingEnabled ~= nil then EnvyAimbot.swingEnabled = cfg.envySwingEnabled end
	
	if cfg.duelCountdownEnabled then State.duelCountdownEnabled = true end
	if cfg.duelDir then duelDir = cfg.duelDir end

	if cfg.uiScale then
		State.uiScale = math.clamp(cfg.uiScale, 0.5, 1.5)
		if globalScaleObj then globalScaleObj.Scale = State.uiScale end
	end
	if cfg.windowScale then
		State.windowScale = math.clamp(cfg.windowScale, 0.5, 1.5)
		if windowScaleObj then windowScaleObj.Scale = State.windowScale end
		recenterWindow()
	end

	local function applySetter(key, value)
		local setter = _G.toggleSetters and _G.toggleSetters[key]
		if setter and value ~= nil then
			setter(value)
		end
	end
	applySetter("stretchRez", cfg.stretchRez)
	applySetter("nightTime", cfg.nightTime)
	applySetter("purpleSky", cfg.purpleSky)
	applySetter("fpsBoosterCursed", cfg.fpsBoosterCursed)
	applySetter("playerEsp", cfg.playerEsp)
	applySetter("antiLagClean", cfg.antiLagClean)
	applySetter("cloudyTime", cfg.cloudyTime)
	applySetter("optimizerEnabled", cfg.optimizeEnabled)
	applySetter("xrayEnabled", cfg.xrayEnabled)

	if cfg.animPackActive and _G._nightmareAnimPackSetters then
		local setter = _G._nightmareAnimPackSetters[cfg.animPackActive]
		if setter then
			task.delay(0.5, function() pcall(function() setter(true) end) end)
		end
	end

	if cfg.showIntro ~= nil then
		State.showIntro = cfg.showIntro
		if setShowIntroToggle then setShowIntroToggle(cfg.showIntro) end
	end

	if cfg.bypassPower then
		applyBypassPower(cfg.bypassPower)
	end
	if cfg.bypassActivated then
		bypassActivated = true
		if setBypassToggle then setBypassToggle(true) end
		startBypassLag()
	end
end

-- ============================================================
-- HIT COUNTDOWN (3-2-1 BAT!)
-- ============================================================
local hitBillboard = nil
local hitCountdownActive = false

local function showHitCountdown()
	if hitCountdownActive then return end
	hitCountdownActive = true
	
	local char = LP.Character
	if not char then
		hitCountdownActive = false
		return
	end
	
	local head = char:FindFirstChild("Head")
	if not head then
		hitCountdownActive = false
		return
	end
	
	if hitBillboard then
		pcall(function() hitBillboard:Destroy() end)
		hitBillboard = nil
	end
	
	local bill = Instance.new("BillboardGui")
	bill.Name = "DARK ANGELHUBHitCountdown"
	bill.Adornee = head
	bill.Size = UDim2.new(0, 150, 0, 80)
	bill.StudsOffset = Vector3.new(0, 2.5, 0)
	bill.AlwaysOnTop = true
	bill.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
	bill.Parent = head
	
	local lbl = Instance.new("TextLabel", bill)
	lbl.Size = UDim2.new(1, 0, 1, 0)
	lbl.BackgroundTransparency = 1
	lbl.Font = Enum.Font.GothamBlack
	lbl.TextSize = 72
	lbl.TextColor3 = Color3.fromRGB(255, 255, 255)
	lbl.TextStrokeTransparency = 0
	lbl.TextStrokeColor3 = Color3.fromRGB(0, 0, 0)
	lbl.Text = "3"
	
	local nums = {"3", "2", "1"}
	for _, n in ipairs(nums) do
		lbl.Text = n
		lbl.TextSize = 40
		TweenService:Create(lbl, TweenInfo.new(0.15, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {TextSize = 72}):Play()
		task.wait(1)
	end
	lbl.Text = "BAT READY"
	lbl.TextColor3 = Color3.fromRGB(255, 255, 255)
	lbl.TextSize = 28
	TweenService:Create(lbl, TweenInfo.new(0.15, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {TextSize = 52}):Play()
	task.wait(0.8)
	
	pcall(function() bill:Destroy() end)
	hitBillboard = nil
	hitCountdownActive = false
end

task.spawn(function()
	while true do
		task.wait(0.05)
		pcall(function()
			local char = LP.Character
			if not char then return end
			local hum = char:FindFirstChildOfClass("Humanoid")
			if not hum then return end
			local st = hum:GetState()
			local isRagdolled = st == Enum.HumanoidStateType.Physics or st == Enum.HumanoidStateType.Ragdoll or st == Enum.HumanoidStateType.FallingDown
			if isRagdolled and not hitCountdownActive then
				task.spawn(showHitCountdown)
			end
		end)
	end
end)

-- ============================================================
-- PLAYER ESP (RED BOX OUTLINE - ALL ENEMIES)
-- ============================================================
local espEnabled = false
local espConnections = {}
local espHighlights = {}

local ESP_COLOR = Color3.fromRGB(255, 255, 255)

local function removeESPForPlayer(player)
	if espHighlights[player] then
		pcall(function() espHighlights[player]:Destroy() end)
		espHighlights[player] = nil
	end
end

local function addESPForPlayer(player)
	if player == LP then return end
	removeESPForPlayer(player)
	local char = player.Character
	if not char then return end
	local hl = Instance.new("SelectionBox")
	hl.Name = "DARK ANGELHUB_ESP"
	hl.Color3 = ESP_COLOR
	hl.SurfaceColor3 = ESP_COLOR
	hl.SurfaceTransparency = 0.85
	hl.LineThickness = 0.06
	hl.Adornee = char
	hl.Parent = char
	espHighlights[player] = hl
end

startESP = function()
	if espEnabled then return end
	espEnabled = true
	for _, player in ipairs(Players:GetPlayers()) do
		if player ~= LP then
			addESPForPlayer(player)
			local conn = player.CharacterAdded:Connect(function()
				task.wait(0.5)
				if espEnabled then addESPForPlayer(player) end
			end)
			table.insert(espConnections, conn)
		end
	end
	local joinConn = Players.PlayerAdded:Connect(function(player)
		if not espEnabled then return end
		local conn = player.CharacterAdded:Connect(function()
			task.wait(0.5)
			if espEnabled then addESPForPlayer(player) end
		end)
		table.insert(espConnections, conn)
	end)
	table.insert(espConnections, joinConn)
	local leaveConn = Players.PlayerRemoving:Connect(function(player)
		removeESPForPlayer(player)
	end)
	table.insert(espConnections, leaveConn)
end

stopESP = function()
	espEnabled = false
	for _, conn in ipairs(espConnections) do
		pcall(function() conn:Disconnect() end)
	end
	espConnections = {}
	for player, _ in pairs(espHighlights) do
		removeESPForPlayer(player)
	end
	espHighlights = {}
end

-- CHARACTER SETUP
local speedLbl = nil

local function setupChar(char)
	task.wait(0.1)
	h=char:WaitForChild("Humanoid",5)
	hrp=char:WaitForChild("HumanoidRootPart",5)
	if not h or not hrp then return end
	local head=char:FindFirstChild("Head")
	if head then
		local oldBB=head:FindFirstChild("DARK ANGELHUBBB")
		if oldBB then oldBB:Destroy() end
		local oldSpeedBB=head:FindFirstChild("DARK ANGELHUBSpeedBB"); if oldSpeedBB then oldSpeedBB:Destroy() end
		local bb=Instance.new("BillboardGui",head); bb.Name="DARK ANGELHUBSpeedBB"
		bb.Size=UDim2.new(0,160,0,52); bb.StudsOffset=Vector3.new(0,3,0); bb.AlwaysOnTop=true
		speedLbl=Instance.new("TextLabel",bb); speedLbl.Name="SpeedBillLbl"
		speedLbl.Size=UDim2.new(1,0,0,24); speedLbl.Position=UDim2.new(0,0,0,0); speedLbl.BackgroundTransparency=1
		speedLbl.Text="0.0"; speedLbl.TextColor3=Color3.fromRGB(255,255,255)
		speedLbl.Font=Enum.Font.GothamBlack; speedLbl.TextScaled=true
		speedLbl.TextStrokeTransparency=0; speedLbl.TextStrokeColor3=Color3.new(0,0,0)
	end
	if Conns.unwalk then Conns.unwalk:Disconnect(); Conns.unwalk=nil end; unwalkAnimateRef=nil
	if State.unwalkEnabled then task.wait(0.3); startUnwalk() end
	stopAntiRagdoll(); if State.antiRagdollEnabled then task.wait(0.5); startAntiRagdoll() end
	if State.medusaCounterEnabled then setupMedusaCounter(char) end
	if EnvyAimbot.enabled then stopEnvyAimbot(); task.wait(0.2); startEnvyAimbot() end
	if State.batCounterEnabled then task.wait(0.3); startBatCounter() end
end
LP.CharacterAdded:Connect(setupChar)
if LP.Character then task.spawn(function() setupChar(LP.Character) end) end

-- RUNTIME LOOPS
RunService.Stepped:Connect(function()
	for _,p in ipairs(Players:GetPlayers()) do
		if p~=LP and p.Character then
			for _,part in ipairs(p.Character:GetChildren()) do
				if part:IsA("BasePart") then part.CanCollide=false end
			end
		end
	end
end)

UIS.JumpRequest:Connect(function()
	if not State.infJumpEnabled then return end
	if State.infJumpMode ~= "manual" then return end
	local c=LP.Character; if not c then return end; local root=c:FindFirstChild("HumanoidRootPart")
	if root then root.Velocity=Vector3.new(root.Velocity.X, State.jumpPower, root.Velocity.Z) end
end)

-- Movement (Fears src - smooth speed with lerp)
local _lastSpeed = 0
RunService.RenderStepped:Connect(function()
	if not (h and hrp) then return end
	if State._tpInProgress then return end
	if not EnvyAimbot.enabled and not State.autoLeftEnabled and not State.autoRightEnabled then
		local md = h.MoveDirection
		local targetSpd
		if State.laggerEnabled then targetSpd = State.laggerSpeed
		elseif State.speedToggled then targetSpd = State.carrySpeed
		else targetSpd = State.normalSpeed end
		local smoothFactor = 0.85
		_lastSpeed = _lastSpeed * smoothFactor + targetSpd * (1 - smoothFactor)
		local spd = _lastSpeed
		if md.Magnitude > 0 then
			State.lastMoveDir = md
			hrp.Velocity = Vector3.new(md.X * spd, hrp.Velocity.Y, md.Z * spd)
		elseif State.antiRagdollEnabled and State.lastMoveDir.Magnitude > 0 then
			local anyHeld = false
			for key in pairs(MOVE_KEYS) do if UIS:IsKeyDown(key) then anyHeld = true; break end end
			if anyHeld then hrp.Velocity = Vector3.new(State.lastMoveDir.X * spd, hrp.Velocity.Y, State.lastMoveDir.Z * spd) end
		end
	end
	pcall(function()
		if speedLbl then
			local hspd = Vector3.new(hrp.Velocity.X, 0, hrp.Velocity.Z).Magnitude
			speedLbl.Text = string.format("%.1f", hspd)
		end
	end)
end)

RunService.Heartbeat:Connect(function()
	if not State.infJumpEnabled then return end
	if State.infJumpMode ~= "hold" then return end
	local char = LP.Character
	if not char then return end
	local root = char:FindFirstChild("HumanoidRootPart")
	if not root then return end
	local hum = char:FindFirstChildOfClass("Humanoid")
	local jumpHeld = UIS:IsKeyDown(Enum.KeyCode.Space) or (hum and hum.Jump)
	if jumpHeld then
		root.Velocity = Vector3.new(root.Velocity.X, State.jumpPower, root.Velocity.Z)
	end
end)

-- AUTO TP DOWN LOOP
RunService.Heartbeat:Connect(function()
	if not State.autoTpDownEnabled then return end
	local char = LP.Character
	if not char then return end
	local root = char:FindFirstChild("HumanoidRootPart")
	if not root then return end
	local curY = root.Position.Y
	if curY >= State.autoTpDownY then
		local rot = root.CFrame.Rotation
		root.CFrame = CFrame.new(root.Position.X, -8.80, root.Position.Z) * rot
	end
end)

-- INPUT HANDLER
UIS.InputBegan:Connect(function(inp,gp)
	if gp then return end
	local isKb=inp.UserInputType==Enum.UserInputType.Keyboard
	local isGp=inp.UserInputType==Enum.UserInputType.Gamepad1 or inp.UserInputType==Enum.UserInputType.Gamepad2 or inp.UserInputType==Enum.UserInputType.Gamepad3 or inp.UserInputType==Enum.UserInputType.Gamepad4
	if not isKb and not isGp then return end
	local kc=inp.KeyCode; if kc==Enum.KeyCode.Unknown then return end
	if kc==Keys.speed then
		State.speedToggled=not State.speedToggled
		if stackBtnRefs.carrySpeed then stackBtnRefs.carrySpeed.setOn(State.speedToggled) end
	elseif kc==Keys.autoLeft then
		if State.autoRightEnabled then State.autoRightEnabled=false; stopAutoRight(); if stackBtnRefs.autoRight then stackBtnRefs.autoRight.setOn(false) end end
		if EnvyAimbot.enabled then setEnvyAimbotState(false); if stackBtnRefs.aimbot then stackBtnRefs.aimbot.setOn(false) end end
		if not State.autoLeftEnabled then
			State.autoLeftEnabled=true
			if stackBtnRefs.autoLeft then stackBtnRefs.autoLeft.setOn(true) end
			startAutoLeft()
		end
	elseif kc==Keys.autoRight then
		if State.autoLeftEnabled then State.autoLeftEnabled=false; stopAutoLeft(); if stackBtnRefs.autoLeft then stackBtnRefs.autoLeft.setOn(false) end end
		if EnvyAimbot.enabled then setEnvyAimbotState(false); if stackBtnRefs.aimbot then stackBtnRefs.aimbot.setOn(false) end end
		if not State.autoRightEnabled then
			State.autoRightEnabled=true
			if stackBtnRefs.autoRight then stackBtnRefs.autoRight.setOn(true) end
			startAutoRight()
		end
	elseif kc==Keys.drop then
		if not State.dropEnabled then runDropBrainrot() end
	elseif kc==Keys.lagger then
		State.laggerEnabled = not State.laggerEnabled
		if stackBtnRefs.lagger then stackBtnRefs.lagger.setOn(State.laggerEnabled) end
		if State.laggerEnabled then
			State._prevCarry = State.carrySpeed
			State._prevSpeed = State.speedToggled
			State.speedToggled = false
			if stackBtnRefs.carrySpeed then stackBtnRefs.carrySpeed.setOn(false) end
			if carryBox then carryBox.Text = tostring(State.laggerSpeed) end
		else
			State.carrySpeed = State._prevCarry or 30
			State.speedToggled = State._prevSpeed or false
			if carryBox then carryBox.Text = tostring(State.carrySpeed) end
			if stackBtnRefs.carrySpeed then stackBtnRefs.carrySpeed.setOn(State.speedToggled) end
		end
	elseif kc==Keys.tpDown then
		doTpDown()
	elseif kc==Keys.aimbot then
		local newState = not EnvyAimbot.enabled
		setEnvyAimbotState(newState)
		if stackBtnRefs.aimbot then stackBtnRefs.aimbot.setOn(newState) end
		if setBatAimbot then setBatAimbot(newState) end
		if newState then
			if State.autoLeftEnabled then State.autoLeftEnabled=false; stopAutoLeft(); if stackBtnRefs.autoLeft then stackBtnRefs.autoLeft.setOn(false) end end
			if State.autoRightEnabled then State.autoRightEnabled=false; stopAutoRight(); if stackBtnRefs.autoRight then stackBtnRefs.autoRight.setOn(false) end end
		end
	elseif kc==Keys.guiHide then
		if isKb then if menuVisible then hideMenu() else showMenu() end end
	end
end)

-- INIT
loadButtonPositions()
loadConfig()

task.delay(1, function()
	pcall(saveConfig)
end)

print("[DARK ANGEL HUB] MEGA AUTO GRAB + ENVY AIMBOT | BLACK + CRIMSON RED THEME")
print("[DARK ANGEL HUB] UI Scale (whole GUI) + Window Scale (main menu only) now available in Settings.")